{
  "name": "MetalliX Pocket Model",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "dev-server",
      "command": "PATH=\"$PWD/.venv/bin:$PATH\" npm run dev",
      "description": "MetalliX unified server (Express + Vite SPA on port 3000) with the persistent Python solver IPC daemon. The .venv is placed first on PATH so the Node server spawns the venv's python3 for the scientific solvers."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
