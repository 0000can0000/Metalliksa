# Metalliksa Proje Durumu (STATUS)

*Bu dosya projenin anlık durumunu, tamamlanan entegrasyonları ve sıradaki hedefleri tutar.*

## 2026-09-25 — P4 current 40 W ve kod-provenance ayrımı
- Dondurulmuş 6 koşuluk IN718 CPU P4 tamamlandı; rapor `docs/LPBF_P4_CURRENT_MODEL_40W_2026-09-25.json`. Enerji kapanışı geçti (maksimum bağıl hata `1.63e-13`); ağ genişlik/derinlik kabulü başarısız (20/10/5 µm düzeylerinde hücre-uzantı genişliği 80/80/80 µm, derinlik 20/30/35 µm; en ince çift derinlik değişimi %14.29). Kontur destek metriği ağ için inconclusive; zaman ağı serisinde gerçek ortalama ilk adım 89.217 ns olduğundan sabit oranlı yakınsama kurulamadı. Fiziksel deney doğrulaması yok.
- `fingerprint(p,m)` önbellek/iş kuyruğu kimliği olarak değişmeden bırakıldı. Ayrı `implementation_fingerprint()` artık solver kaynak ağacı ve model sürümünü girdilerden bağımsız tanımlıyor; CPU, CUDA-pilot ve mühendislik benchmark sonuçlarındaki `implementationHash` bunu kullanıyor. Böylece farklı parametrelerle üretilen çıktıların aynı kod sürümünü göstermesi sağlanırken cache ayrımı korunuyor.
- Doğrulama: Python `py_compile`, `git diff --check`, cache input-separation assertion ve iki farklı girdiyle provenance regresyonu geçti. Birleştirilmiş cache/queue regresyonu hash kontrollerini geçti, ardından SQLite geçici veritabanı oluşturma/açma `WinError 5` erişim kısıtında kaldı; ilgili koşu tamamlanamadı.
- Commit `15b4663` kaydedildi. P4 incelemesi ayrık ölçüm operatörünün hücre genişliği ekini ve derinlik nicemlenmesini gösterdi; fizik denklemi arızası kanıtlanmadı. Salt tanı protokolünü aşağıdaki ayrı kayıt izler. Ardından P5 altı-kesit operatörü ve ışın profili kapısını sürdür; yeni fizik denklemi ancak somut hata kanıtı ve analitik/parite doğrulamasıyla değiştirilsin.

## 2026-09-25 — P4 40 W eşit-tepe zamanı tanı protokolü
- Sonuç `docs/LPBF_P4_CURRENT_40W_TIED_ENDPOINT_DIAGNOSTIC_2026-09-25.json`; protokol SHA-256 `c5628e46eed810e6de7638742e2c15f1b94e60f1386df07cf1241c960d5b88ee`, rapor SHA-256 `7a45ce57645b7b0bb88fe7a844dfdaffbae770d7872f62bcee3a5ac68e493235`. 20/10/5 µm ağlarda 2349/190/7 eşit maksimum adımın tamamı geçerliydi; üretim konturu ile bağımsız oracle her seviyede min/medyan/maksimum için tam eşleşti. Kontur yayılımı (W;D µm): 20 µm 70.689–72.477; 28.623–29.656. 10 µm 74.328–74.518; 32.811–33.332. 5 µm 77.7036–77.7063; 34.6929–34.6963. Aralıklar mesh seviyeleri arasında örtüşmüyor; tie-zaman seçimi mesh trendini açıklamıyor. Toplam 3379.94 s; yalnız tanı maliyeti.
- Önemli sınır: tanı runner'ı yeni enerji defteri yazmadı. Eşleşen enerji değerleri aynı girdi seviyeleri için frozen P4 raporundan açıkça etiketlenerek referanslandı (bağıl hata `1.63e-13`, `1.63e-13`, `1.63e-13`); bu tanı koşusunun yeni enerji ölçümü değildir. Gerçek yürütme HEAD'i protokol commit'i `476a55e`; protokoldeki `15b4663` kaynak commit'inden bu çalıştırma başlangıcına kadar yalnız durum/plan belgeleri değişti. Solver uygulama SHA-256 `72520e2a25c5a6b781e2c7a0bb8c501deadfe2d6c15ce73e017bbd22e56459d3` protokolle aynı.
- Frozen P4 `failed`/`inconclusive` ve `experimentalValidation=false` kaldı. Bu tanının kabul durumu `not-assessed`; hiçbir bilimsel eşiği değiştirmedi. Sıradaki somut iş: NIST v1 thermal-proxy yolunu aynen koruyarak ham altı resmi gözlemle versioned v2 operator adayını tasarla; ölçülmüş ışın profili/fusion-zone operator kanıtı olmadan residual'ları validasyon diye sunma.

## 2026-09-25 — Build-job material revision response contract
- Python build-job solver artık `materialPropertyRevision` alanını üst seviye yanıtta da yayımlıyor; önceden alan yalnızca `buildJobIdentity` içinde bulunuyordu ve istemci yanıtı geçersiz sayıyordu. Python sözleşme assertion'ı ve eksik üst seviye alanı reddeden istemci regresyonu eklendi.
- Doğrudan Windows Python 3.12 gerçek-solver çağrısı başarılı oldu; üst seviye revizyon ve kimlik revizyonu `build-job-effective-properties-v1` olarak eşleşti. Odaklı istemci paketi izinli runner'da **13/13 PASS**; ilk sandbox denemesi `spawn EPERM` ile kısıtlanmıştı. `git diff --check` temiz.
- Sıradaki adım: odaklı Node testi izinli runner'da çalıştır; kontrollü P4 koşusunun 5 µm ağ ve zaman adımlarını tamamlayıp dondurulmuş kapılara göre raporunu değerlendir.

## 2026-09-25 — OpenFOAM layer-conforming powder grid
- İnceleme, eski OpenFOAM powder-layer ağının katman yüzeylerini hücre yüzlerine hizalamadığını ve Gauss kaynak kaybını yeniden-normalize ettiğini buldu. Ortak `calculate_mesh_domain` hesabı tekdüze kübik ağda hizalamayı destekliyor; `standard/openfoam-thermal` şimdi sürümlü `layer-conforming` politikasını alıyor. Vaka üreticisi politika ve yüz indekslemesini yazmadan önce doğruluyor.
- OpenFOAM C++ kaynak integrali, NumPy referansındaki `1/1.01` minimum yakalama sınırını yeniden-normalizasyondan önce uyguluyor. Python sonuç kapısı da kaynak yakalamasını ve raporlanan maksimum yüz ofsetini doğruluyor; uyumsuz ikili sonucu kabul edilmiyor.
- Doğrulama: core-contract **16/16 PASS**; mühendislik+heat-source grubu **49 PASS, 2 OpenFOAM skip**; güncellenen policy testi ile core/model ve case-grid odak testleri **3/3 PASS**. Geniş test koşusunda tek başarısızlık eski hata mesajı beklentisiydi ve yeni sözleşmeye göre düzeltildi. WSL erişimi `E_ACCESSDENIED`; Windows'ta `wmake` ve Docker OpenFOAM imajı yok, dolayısıyla C++ ikilisi ve gerçek solver henüz derlenip çalıştırılmadı.
- Sıradaki somut adım: WSL/OpenFOAM erişimi olan işçide hizalı çok katmanlı vakayı derleyip çalıştırmak; yüz ofseti, yakalama, enerji kapanışı ve NumPy metrik/alan paritesini doğrulamadan OpenFOAM yolu tamamlanmış sayılmayacak.

## 2026-09-25 — P6 opt-in CUDA kaynak prototipi
- Aynı `enthalpy-fv-6` standart tek-iz altkümesinde GL2 hareketli Gauss hücre integrali ve 25 K kaynak-adımı sınırlayıcısının opt-in CUDA uygulaması eklendi. Hücre kaynak/rate alanı CPU oracle’ına `rtol=1e-10`, yakalama farkı `1e-12`, `dt` farkı `1e-15` içinde eşleşti; retry sayısı aynı ve dondurulmuş tam CPU/CUDA sıcaklık-alanı paritesi değişmedi.
- Üç eşzamanlı/alternating 934-adım ölçümünde CPU-kaynak medyanı `6.722 s`, CUDA-kaynak medyanı `9.062 s`: CUDA kolu yaklaşık `%35` yavaş. Batch GL2 düğümleri ve tekleştirilmiş capture/`dt` senkronizasyonu önceki CUDA medyanını `10.171 s`'den iyileştirdi; hız kazancı kanıtlanmadı. Kuyruk/varsayılan kaynak ve sınırlayıcı CPU kalıyor.
- Doğrulama: `python/test_lpbf_gpu_thermal.py` **9/9 PASS** (RTX 4060 paritesi dahil), `tsc --noEmit` PASS, `git diff --check` PASS. Commit `a3d8aaa`. P6 kısmi; bu yazılım/sayısal parite deneysel doğrulama değildir.
- P4 tied-peak denetimi, ilk eşit maksimum adımın mevcut deterministik seçimini ve zaten mevcut opt-in tied-contour duyarlılık yolunu doğruladı; `python/test_lpbf_peak_tied_contours.py` **2/2 PASS**. Dondurulmuş kabul ölçütleri/sonuç değişmedi.
- Kullanıcı, yetersiz sonuç veren fizik motorlarının Python'da bilimsel temelle onarılmasına veya yeniden kurulmasına açıkça yetki verdi. Denklemleri ve model kimliklerini gerekçesiz değiştirme; kabul eşiklerini gevşetme; önce analitik/üretilmiş çözüm ve sayısal pariteyle sınayıp fiziksel deney kanıtından ayrı raporla.
- Sonraki adımlar: uygun source-bound archive koşuları mevcut olduğunda campaign round-trip'i canlı doğrula; P6'da kararlı uçtan uca hız kazancı ölçmeden GPU'yu varsayılanlaştırma; P4'ün `2.5 µm` koşusunu ölçülmüş yarar ve aynı parite kapısı olmadan çalıştırma.

## 2026-09-25 — NIST proxy kampanya arşiv/panel dilimi
- Üç arşivlenmiş transient koşu ve Table 4 case seçimi, server-side preview ve değişmez campaign kaydı UI/client akışına bağlandı. Client'a yalnızca run ID'leri, case ve preview SHA gider; sayısal ölçü, residual ve validasyon iddiası gitmez. Panel altı gözlemi yalnızca thermal-proxy screening/unvalidated olarak gösterir.
- Server/API/persistence/bundle: `d967f87`; UI/client ve iki odaklı test: `7004742`. `7cfba1a` arşivlenmiş kampanyaları listeleyip tekrar açmayı ve client'ta bundle v2 `campaignCount` doğrulamasını ekledi; v1 bundle okuması sürüyor.
- Doğrulama: tam `tsc --noEmit` PASS; `python/test_lpbf_gpu_thermal.py` **9/9 PASS**; Node depo/bundle/API/client/UI odak paketi **48/48 PASS** (alt süreçler sandbox dışında çalıştırıldı); `git diff --check` PASS. Proxy campaign list servisi ve run/source referans reddi sentetik arşivle doğrulandı.
- Canlı kullanım sınırı: uygun, source-revision-bound üç koşuyla gerçek NIST preview/create henüz çalıştırılmadı. Bundle round-trip testi campaign fixture'ıyla geçti; P5 residual ve deneysel validasyon hâlâ `unavailable`.
- P5 optik residual/validasyonu `unavailable`; proxy campaign bunu değiştirmez. Uygun arşiv verisiyle canlı preview→create→reopen→export/verify/restore entegrasyon doğrulaması açık.

## 2026-09-25 — P6 ölçek ölçümü ve motor onarım yetkisi
- Kullanıcı, bilimsel olarak yetersiz bulunan çekirdek fizik motorlarının onarılmasına veya gerektiğinde Python'da yeniden kurulmasına açıkça yetki verdi. Uygulama: fizik modeli/denklem kaynağını ve kimliğini gerekçesiz değiştirme; kaynak yakalama, enerji, parite ve P4 eşiklerini gevşetme; sayısal kanıtı fiziksel deney kanıtı gibi sunma.
- 10 µm, 73,568 hücre, 934 adım IN718 tam CPU/GPU alan paritesi her iki kaynak yolunda **PASS**: alan yükselişinin L2/max bağıl farkı `2.60e-9` / `7.28e-9`; enerji ve diğer parite kapıları geçti. Hız ölçümü kararlı değil: bir seride CPU `11.4155 s`, GPU+CPU-kaynak `9.4337 s`; ısınmalı seride CPU `9.6377 s`, GPU+CPU-kaynak `9.5839 s` (GPU örnekleri `8.2945–9.7906 s`). `compare_with_cpu` toplam süreleri iki motoru da çalıştırdığı için hız kıyası değildir. GPU hız kazancı bu ağda tekrarlanabilir kanıtlanmadı. CUDA kaynak entegrasyonu CPU kaynak yolundan medyanda yalnızca `0.01 s` farklıydı; anlamlı kazanç yok.
- 20 µm, 9,196 hücrede üç dönüşümlü CPU-kaynaklı GPU medyanı ilk seride `6.8944 s`, ısınmalı seride `7.0524 s`; CPU referansı `3.3405 s` / `3.2946 s`. Bu ağda GPU yaklaşık `2.1x` yavaş. P6 hızlanma iddiası ve mesh-refinement'a taşıma açık; senkronizasyon birleştirme denemesi tutarlı kazanç vermediği için geri alındı.
- 5 µm tam çözücü ölçümü, GPU pilotunun mevcut `MAX_CELLS=100000` bütçe korumasında durdu; bu ağaç için tam-çözücü 5 µm crossover kanıtı yok. Daha önceki kaynak/limitleyici mikro ölçümleri yalnızca bileşen ölçümüdür.
- Ayrı IN718 CPU tanı koşusu `6.6667 µm` gerçek ağda tamamlandı: `222,156` hücre, `14,000` adım, genişlik `80 µm`, derinlik `33.333 µm`, eriyik hacmi `279,111.1 µm³`, tepe sıcaklığı `2590.16 K`; kaynak yakalama `0.9999999990`, yüzey ofseti `~0 µm`, enerji bağıl hatası `1.63e-13`. Bu tek, sonuç-sonrası ince ağ tanısıdır; dondurulmuş P4 çalışmasına eklenmedi ve yakınsama kabulü/deneysel doğrulama sayılmaz. 10 µm raporuyla birebir girdi özdeşliği ve fark ölçütü doğrulanana kadar doğrudan yüzde kıyası yapma.
- Üretim liquidus-kontur operatörü, analitik ve ızgara-dışı merkezli 3B küre ile tarama yönünde döndürülmüş elipsoitte 20/10/5/2,5 µm aralıklarında sınandı. Genişlik ve derinlik mutlak hataları her inceltmede azaldı; 2,5 µm'de en büyükleri sırasıyla `0.044` ve `0.0193 µm`. `python/test_lpbf_interpolated_section.py` **5/5 PASS**. Bu yalnızca geometrik ölçüm operatörü doğrulamasıdır; termal çözücü/deney doğrulaması değildir ve P4 kapısını değiştirmez. Mevcut kanıtla ısı denklemini yeniden yazmak gerekçeli değil.
- Fiziksel doğrulama hâlâ yok; sonuçlar aynı-model yazılım/sayısal CPU-GPU paritesidir. CUDA kaynak yolu opt-in, CPU varsayılanı olarak kalır. Commit: henüz yok (bu bölüm dokümantasyon devam kaydıdır).

## 2026-09-25 — Toz-katmanı yüzey geometri düzeltmesi
- Bilimsel incelemede hücre-merkezi maskesiyle etkinleştirilen yüzey ile gerçek katman yüzeyinde kesilen Gauss kaynağının farklı geometri kullandığı doğrulandı. `standard/reference` toz-katmanı koşuları varsayılan olarak mevcut sürümlü `layer-conforming` ağı kullanıyor; `backend=auto` bu model için NumPy referansa çözülüyor. Eski arşivlenmiş `stationary-enthalpy-conduction-v1` sonuçları değiştirilmedi; yeni sonuçlar `stationary-enthalpy-conduction-layer-conforming-v1` kimliğini ve gerçek ağ ayarını taşıyor. GPU pilotu aynı model kimliğini/ızgarayı kullanıyor. Açıkça seçilen OpenFOAM akışı bu değişikliğin kapsamı dışında.
- IN718, 80 µm katman ve 25 µm istekli üç-katman örneğinde eski hücre-merkezli uygulama kaynak yakalama `%86.763` ile mevcut `%99` kapısında reddedildi. Yeni hizalı çözüm gerçek `20 µm` hücre, `0 µm` üst-yüzey ofseti, `13,068` hücre, 2,602 adım ve enerji kapanış bağıl hatası `2.57e-14` ile tamamlandı. Kaynak/enerji/parite eşikleri değiştirilmedi.
- Kontroller: mühendislik grubu **36 PASS, 1 OpenFOAM worker skip**; core-contract **15/15 PASS**; CUDA termal **9/9 PASS**; heat-source **14 PASS, 1 OpenFOAM skip**; gerçek CUDA queue **3/3 PASS**; `tsc --noEmit` ve odaklı GPU/contract Node testleri **5/5 PASS**. API paketi **5/6**; kalan test gerçek OpenFOAM capability bekliyor ve Windows worker'da unavailable. Deneysel doğrulama yapılmadı.
- Senkronizasyon birleştirme mikro-optimizasyonu eklenip aynı ağlarda ölçüldü; istikrarlı iyileşme göstermediği için kaynak kodundan geri alındı. P6 hızlanma iddiası açık kalıyor.

## 📌 Genel İlerleme Özeti
- **Python Fizik Motoru:** `ROADMAP.md`'ye göre Faz 1'den **Faz 21 (Transient Enthalpy-Method Phase-Change)** aşamasına kadar tüm analitik ve GPU (Warp) tabanlı fizik/simülasyon çekirdekleri yazılmıştır (`python/` dizini).
- **Backend (API) ve Frontend (UI) Entegrasyonları:** Çekirdek fizik motorlarının son kullanıcıya ve arayüze bağlanma süreci devam etmektedir. Yakın zamanda Faz 8, 9, 10 ve Faz 14 entegrasyonları tamamlanmıştır.

## 2026-09-25 — Üretim transient motoru ve CUDA istek kapısı
- Üretim CPU `transient()` döngüsünü kullanan uniform ve uniform olmayan analitik entalpi kontrolleri eklendi. Üç ayrı zaman adımında doğru final sıcaklık profilleri üretildi; uzaysal kontrol her adımın pasif iletim/sınır kaybını bağımsız yüz-akısı hesabıyla karşılaştırdı. Üretim kaynak-zaman-sınırlama/yeniden deneme yolu çalıştı ve enerji kapanışı bağıl hatası `1e-10` altında kaldı. Bu, gerçek geçici yolu ve ayrık iletim uygulamasını doğrular; deneysel doğrulama değildir.
- CUDA pilotu, farklı model kimliği gerektiren `layer-conforming` grid talebini cihaz/mesh işinden önce açıkça reddediyor; standart/reference isteği kabul regresyonu eklendi. Fizik denklemleri değişmedi.
- Doğrulama: GPU parite + iki üretim transient testi **9/9 PASS** (IN718 ve 316L CUDA pariteleri dahil); transient/kaynak/yakınsama grubu **26 PASS, 1 Linux-only OpenFOAM skip**. P4/P5/P6/P7 kapıları değişmedi; GPU parite hızlanma veya malzeme/deney yeterliliği değildir.
- Sonraki adım: aynı `enthalpy-fv-6` fiziğinde CUDA/Warp ölçek ve hız-kesişme sınırını ölçmek; yalnızca parite korunursa 2,5 µm P4 ağı için kullanmak. Dondurulmuş P4 eşikleri değişmeyecek. Devam kaydı: `docs/ACTIVE_WORK.md` son bölümü.

## 2026-09-25 — Üç ağlı termal çalışma düzeltmesi
- Built-in standart powder-layer mesh study, katman yüzeylerini hücre yüzlerine hizalayan CPU-reference protokolüne geçirildi. Kaynak yakalama %99 eşiği ve yeniden ölçekleme davranışı değişmedi; `auto` istek ile fiilen kullanılan `reference` backend ayrı kaydediliyor.
- Önceki hata 28 µm orta seviyedeydi: [28,56] µm z hücresi 40 µm katman yüzeyini kesiyor, hücre merkezi 42 µm olduğu için mevcut whole-cell etkinlik kuralı hücreyi dışarıda bırakıyordu. Bu yüzden kaynak yakalama %54,8506 oldu. Kesilmiş hücre kapasitesi/iletimi eklemeden güvenli çözüm: her seviyede katman başına tam sayı hücre.
- Aynı IN718 30 W / 1200 mm/s / 80 µm vektörü üç seviyeyi tamamladı: 40/20/13,333 µm (1/2/3 hücre/katman), enerji bağıl hatası `3,04e-16`. W/D/hacim yakınsaması `inconclusive`; çalışma doğrulanmamış ve deneysel validasyon değildir.
- Sonuç paneli artık bu protokolde istenen backend'i (ör. automatic) ve çalışan CPU-reference backend'i, ayrıca katman başına hücre sayısını açıkça gösteriyor; `executionInputHash` API tipinde korundu. Hedefli Python regresyonları PASS, `npm run lint` PASS. TSX panel testi `spawn EPERM` yüzünden başlamadı; tarayıcı doğrulaması yapılmadı.
- Bağımsız P4 incelemesi: mevcut 75 W contour vakasında enerji ve mesh-genişlik kapıları PASS; derinlik ve zaman eğilimleri `inconclusive`. 80 W dondurulmuş P4 `failed` kalır. Katman uyumlu model farklı kimliktedir ve eski P4 kabul kanıtının yerine geçmez. Sıradaki adım, yeni altı koşulu çalıştırmadan önce tepe-zamanı/eşit-maksimum duyarlılığını tanısal olarak incelemek.
- Bağımsız P5 incelemesi: arşivde uygun bare-plate IN718 run yok; kaynak revizyonu, süreç vektörü, ölçüm operatörü, ışın profili ve arka plaka sınır koşulları eşleşmiyor. NIST workbook üç izde 4,9 ve 6,0 mm konumlarını doğruluyor. Comparator artık tek 5 mm sentetik/orta-kesit kaydından sayısal residual üretmiyor; altı ayrı, üç simüle iz × iki konum kaydı için operatör henüz uygulanmamış olduğundan P5 `unavailable`. IN625 ortak powder-layer/full-transient kapısı da hâlâ kapalı; ayrı bare-plate CUDA paritesi bunu karşılamıyor.
- Devam: P4 tepe-zamanı tanısını ve yeni protokol dondurmasını sürdür; P5 altı-kesit kaynak/operatör sözleşmesini birincil yöntem kanıtına bağla. Veri ve bilimsel eşleşme tamamlanmadan yeni NIST karşılaştırma koşusu çalıştırma.

## 2026-09-25 — P5 tek-kesit false-positive kapısı
- NIST Table 4 gözlemi üç ayrı izde, her iz için 4,9 ve 6,0 mm'de alınmış altı kesittir. Comparator daha önce tek x=5 mm bölümüne `observationCount=6` verildiğinde test şemasında `comparable-screening` üretebiliyordu; bu, kaynak operatörüyle eşleşmiyordu.
- `python/lpbf_nist_in718_comparison.py` tek-kesit çıktısından artık hiçbir koşulda residual üretmiyor; raporda iki fiziksel konum ve üç iz gereksinimi görünür. Ölçüm operatörü ve üç ayrı simulated-track kaydı uygulanana kadar sonuç `unavailable` kalır.
- `python -m unittest test_lpbf_nist_in718_comparison -v`: 6 test PASS. Bu yalnızca karşılaştırma kapısı regresyonudur; motorun deneysel validasyonu veya NIST kıyası değildir.
- Sıradaki adım: P4 eşit-tepe tanısını sürdür; P5 için üç ayrı izden 4,9/6,0 mm termal proxy kayıtlarını ve ölçüm operatörünü içeren sürümlü bileşik run sözleşmesini tasarla. Koşu maliyeti, kaynak profili ve 10 mm model-geçerlilik duruşu çözülmeden simülasyon başlatma.

## 2026-09-25 — P3/P8 canlı arşiv bundle round-trip
- Mevcut UI'da seçili run ve kaynak arşivi üzerinden bundle dışa aktarıldı, sunucu tarafında byte/reference doğrulaması yapıldı ve canlı arşivden ayrı restore dizinine kopyalandı. UI ve manifest: 3 run, 132 run artifact, 3 source link; restore `2e251b3d73a7470ead671287649ace35`, bundle `be741eebfa12413a8fd78978ecefdfc3`. Canlı arşivdeki 3 run değişmedi.
- Aynı UI'da resmî NIST optik çalışma kitabının revision 1 arşivi seçilip baytları doğrulandı: XLSX 25.811 bayt / SHA-256 `2cfaac96aaca3dabb77b7029f842cdcc7e75c5a2cf3577d0734823246364a931`; yayıncı SHA yan dosyası 64 bayt / SHA-256 `770c0826e53e42c242110e69c032f2cbc74f6183048c43a530c5b62e746ae09b`. Revision ve archive-document SHA: 1 / `73293ca6c2a1929a2e244f806d6eb5900d4c739716f7f291e74c9bc12dc291b6`.
- Aynı UI'da seçili IN718 run'ın Table 4 revision 1 bağı, incelenen yerel revision 2 ile uyuşmadığı için karşılaştırma `unavailable` döndü; hata eski source identity/transcription mismatch olarak görünür. Eski run yeniden bağlanmadı.
- Bu, API/UI export→verify→isolated-restore yazılım akışını doğrular; model geçerliliği veya deneysel karşılaştırma değildir. Geliştirici sunucusunu bu deneme için tekrar başlatma girişimi Windows `spawn EPERM` ve port çakışması verdi; önceden açık Codex içi localhost UI kullanıldı, başlatılan ek IPC süreci kapatıldı.

## 2026-09-25 — P7 kanıt matrisi, motor denetimi ve P8 arşiv kabulü
- IN625 özellik kanıt matrisi `docs/IN625_P7_PROPERTY_EVIDENCE_MATRIX_2026-09-25.md` içinde oluşturuldu. Kaynak/lot/durum/aralık/belirsizlik ayrımı yapıldı; alaşım `thermal-screening-only`, `fullTransient=false`, `buildJob=false` kalıyor.
- Faz 21 motoru tarama hızı içermeyen durağan 2B screening olarak doğrulandı; sabit ışın yarıçapı/soğurma ve 2B güç normalizasyonu eksikleri açıkça raporlanıyor. Worker RPC mevcut, fakat UI servis metodunun indekslenmiş çağrısı bulunmadı. Motor tarama çözünürlüklü eriyik havuzu sonucu gibi sunulmamalı; doğru hareketli kaynak modeli ayrı fizik sözleşmesi gerektirir.
- Standart `python/lpbf_simulation.py::transient` zaten parçalı doğrusal hareketli scan segmentleri, hücre-integralli Gauss kaynak, iki düğümlü zaman kuadratürü ve uyarlamalı açık adım kullanan 3B CPU termal referansıdır; malzeme ızgarası sabittir. Eski 2B `TransientEnthalpyFDMSolver` ayrı stationary screening olarak kalır. Kaynak normalizasyonunda görünen `0.5` güç hatası şüphesi yanlıştı: iki GL2 düğümün ağırlığı toplamda adım başına P verir. `test_symmetry_power_and_future_powder` hedefli çalıştırmada PASS; kaynak alanı yeniden ölçeklemesi yine %99 yakalama kapısıyla sınırlı ve deneysel doğrulama değildir.
- P4 eşit-tepe uçları mesh'e duyarlı olabilir; bu, frozen FAILED sonucunun nedeni olarak kanıtlanmadı. P5'te akış, buharlaşma kütle aktarımı ve kütle/enerji kapanışı eksikleri sürüyor; `unavailable` korunuyor.
- Taze UI'daki "Three meshes" denemesi 28 µm orta seviyede Gauss kaynak yakalamasını %54,851 bulup değişmeyen %99 kapısında doğru biçimde durdu. Bu, kaynağı yeniden ölçekleme veya eşiği düşürme gerekçesi değildir. Çalıştırıcı başarısız seviyeyi açıkça raporlarken istenen sonucu ve diğer geçerli seviyeleri saklıyor; eksik dizinin tüm yakınsama metrikleri `failed` kalıyor. Odaklı regresyon ve 15 ısı-kaynağı testi PASS (1 OpenFOAM atlandı), `npm run lint` PASS; UI testi `spawn EPERM` ile başlayamadı. Geniş Python paketinde Windows temp erişimi yüzünden 7 hata oluştu, 2 OpenFOAM testi atlandı. Commit: `4684213`; geniş paket doğrulanmadı.
- Güncel kaynak `npm run build` ile derlendi; Vite büyük Three.js chunk uyarısı verdi. Derlenmiş sunucu ve Python worker localhost'ta başladı, `/api/health` `ok` döndürdü. Taze UI'da IN625 sınırlı bare-plate koşusu CPU ve CUDA üzerinde aynı model revizyonu/malzeme snapshot'ıyla tamamlandı: tepe sıcaklığı her ikisinde 300.226 K, son entalpi farkı `2.22e-16 J`, alan maksimum sıcaklık farkı `1.14e-13 K`. Bu aynı-girdi cihaz sayısal paritesidir; deneysel doğrulama değildir. Koşu exact local-derived source SHA `be3286b30b3ec3a6970b577cd9050b19de716cbf8754c7ac2355cf20d5cea655` ile arşivlendi ancak core contract legacy/unbound kaldı. Taze IN718 termal koşusu da arşivlendi; 40/20/60 µm eriyik geometrisi düşük çözünürlüklü ve doğrulanmamıştır. IN625 koşusunun IN718 NIST Table 4 ile kıyası eşleşmeyen malzeme/proses sözleşmesi nedeniyle doğru biçimde unavailable verdi.
- Klavye kabulünde arşiv seçicisi ArrowUp/ArrowDown ile değiştirildi ve özgün IN625 koşusuna geri döndürüldü. Sunucu-yerel bundle `5c86ac62fed64f9b93c9a53b8d6817a9` 3 koşu, 132 koşu artefaktı ve 3 kaynak bağlantısıyla üretildi/doğrulandı; izole geri-yükleme `58d94b80d0904a53bb11f5bab73eac51` canlı arşivi değiştirmeden tamamlandı. Bunlar yazılım iş akışı ve bütünlük kanıtıdır; fiziksel model doğrulaması değildir.
- Sıradaki adım: kaba mesh'te kaynak yakalamasının neden düştüğünü %99 kapısını sabit tutarak çöz ve üç seviyeli denemeyi tekrar çalıştır. Ardından frozen P4 ölçütlerini değiştirmeden tied-peak tanısını ve hareketli-kaynak çözücüsünün mesh/zaman hata ölçümünü sürdür; 1–2 hücrelik eriyik boyutları için çözünürlüğü artır ve eşleşmiş bağımsız IN718 verisi ara. P7 full-transient/build-job ve P5 kapıları kanıt geçene dek kapalı kalır.

## 2026-09-24 - LPBF devam noktası: P4/P5/P8
- Üçüncü ve ayrı dondurulmuş katman-uyumlu P4 eşlik koşusu tamamlandı. Protokol ve senaryo sonuçlardan önce `e439a56` ile kaydedildi; senaryo SHA-256 `80c3d4e1c58e28559914ff3a02fc6edc762b8ffe21336466f60b2826fe388ecc`. Altı CPU koşusunun tümü tamamlandı; kaynak-yakalama kapısı geçti ve enerji bağıl hatası en çok `1.286e-13` oldu. Gerçek ağ oranları 26,667/13,333/6,667 µm, zaman oranları 99,982/49,991/24,996 ns ile 2:1:0,5 dizisini sağladı. Mesh ve zaman değerlendirmeleri `inconclusive`: en ince mesh çiftinde ve tüm zaman düzeylerinde ayrık hücre-genişliği/derinliği aynı kaldı. Rapor `docs/LPBF_P4_LAYER_CONFORMING_V2_80W_2026-09-24.json` (SHA-256 `873ea698d00d7deb5db06ad241028b42161ea24e9d6385d3f6953164e270c4ec`). Bu bir katmanlı senaryoya ait CPU sayısal kanıtıdır; deneysel doğrulama değildir ve özgün P4 ya da önceki eşlik sonuçlarını değiştirmez.
- P4 frozen 80 W sonucu değişmedi: genel durum `failed`; mesh genişliği trendi `inconclusive`, derinliğin son çift farkı %17,1875, zaman geometrisi `inconclusive`. İzole tarihsel diagnostic replay altı input hash'ini ve tüm raporlanmış sayısal değerleri sıfır farkla üretti; `implementationHash` eşleşmediğinden exact-code replay sayılmaz. 69 tarihsel commit üzerinde hash taramasında eşleşen kaynak ağacı bulunmadı; özgün koşunun commit dışı/dirty Python ağacından gelmiş olması olasıdır. Eklenen tepe tanıları kaba ağlarda 439/300, ince ağda 25 eşit tepe endpoint'i gösteriyor; olası operatör duyarlılığıdır, nedensellik kanıtı değildir. Ayrıntı: `docs/LPBF_P4_PEAK_DIAGNOSTIC_REPLAY_2026-09-24.md`.
- Güncel solver'ın aynı kaba seviyeyi denemesi yeni %99 Gaussian kaynak-yakalama eşiğinde (%86,763 yakalama) durdu. Eşiği veya P4 kabul ölçütlerini gevşetme. Exact report-time kaynak ağacını `implementationHash` üzerinden bulma incelemesi sürüyor.
- NIST 2025 beam raporunda normalize edilmiş ölçülmüş ışın profili ve merkez x/y kesit grafiği (Fig. 7) vardır; Fig. 5 simüledir. İncelenen açık kayıtlarda ham sayısal 2B kamera dizisi ve tarama-özel kaynak-bayt bağı bulunmadı. `Dg`–`D4σ` eşitliği ideal-Gaussian varsayımı olarak kalır. P5 `unavailable`.
- P8 için en yeni kayıtlı canlı UI akışı `PROOF.md` içindeki IN625 preview/import/byte-verify → açık CPU/CUDA compute/compare → exact-source archive → bundle verify/isolated restore kanıtıdır. Bu yazılım akışını destekler; run `legacy-unbound`, IN625 unvalidated screening, NIST kıyası unavailable.
- NIST Table 4 yerel transkripsiyonunun catalog version 1.1.0 sürümü önizlenip revision 2 olarak içe aktarıldı; tek dosya 4.321 byte, document SHA-256 `6c9d9f80f8c4eb2b7a6c18bbaab9ed7a993e43f155190dfff49808a4f854aaf0`, import sırasında byte eşleşmesi doğrulandı. IN718 arşiv koşusu `aad3bc4b...` değişmez biçimde eski revizyon 1'e bağlı (version 1.0.0, 3.374 byte); güncel karşılaştırıcı eski sabit transkripsiyon artefaktını doğru şekilde reddetti. Bu iş ayrıca 30 W / 1200 mm/s vektörünün seçili NIST Case 0'dan farklı olduğunu gösterdi; karşılaştırma için ölçülmüş AMB2022-03 ışın profili baytları da hâlâ bulunamadı. Yeni veri sürümü tek başına bilimsel kıyas veya doğrulama sağlamaz.
- Katman-yüzeyi hizalı P4 v1 sonucu `docs/LPBF_P4_LAYER_CONFORMING_80W_2026-09-24.json` içinde: altı koşu tamamlandı, enerji PASS (`3.07e-14`); gerçek mesh oranları `2` ve `1.5` olduğundan mesh FAILED, gerçek dt oranları sabit olmadığından zaman INCONCLUSIVE.
- Faktör-2 katman-yüzeyi hizalı eşlik çalışması `9594ea4` ile önceden donduruldu. Altı koşu tamamlandı, enerji PASS (en büyük bağıl hata `1.281e-13`); ilk iki mesh W/D çifti aynı, ince çift değişimi genişlikte %33,33 ve derinlikte %20 olduğundan mesh FAILED. Üç zaman seviyesinin hücre-uzantı W/D metrikleri aynı kaldı; zaman INCONCLUSIVE. Rapor `docs/LPBF_P4_LAYER_CONFORMING_FACTOR_TWO_80W_2026-09-24.json` içinde. Eski frozen P4 ve iki eşlik sonucu birbirinden bağımsız kalır.
- Sıradaki P10 adımı: z=0 arayüzünü koruyarak kaynak/surface hücre uyuşmazlığını fiziksel olarak çözmenin güvenli yolunu araştır; kesilmiş hücre fiziği olmadan genel grid kaydırma yapma. Diğer P0–P10 iş kollarını sürdür. Hiçbir kaynak-yakalama veya P4 kabul eşiği gevşetilmedi.

## 2026-09-24 — Phase 22 serbest-yüzey/entalpi tutarlılığı
- Serbest-yüzey grafiği bir adım ilerletildikten sonra yeni grafiğin üstünde kalan eski-yüzey entalpili hücreler artık ortam entalpisi/sıcaklığına uzlaştırılıyor; düzeltme `surface_mask_reset_J` ile enerji defterine tam bir kez yazılıyor. Son sıvı hacmi hesabı da güncel yüzey grafiğinin üstündeki hücreleri dışlıyor.
- Regresyon yeni grafiğe maruz kalan hücreyi iki kez uzlaştırıp düzeltmenin ikinci kez yazılmadığını, yüzey dışı sıcak hücrenin eriyik hacmine katılmadığını ve hareketli yüzeyli solver koşusunda enerji kapanışının korunmasını denetliyor.
- Doğrulama: `python/test_lpbf_transient_3d_gpu.py` **34/34 PASS**; yeni kernel CPU ve NVIDIA RTX 4060 `cuda:0` üzerinde çalıştı. Entegre sentetik hareketli-yüzey koşusunda göreli enerji artığı `2.79e-4` oldu. Bu sayısal/yazılım tutarlılığı kanıtıdır; deneysel doğrulama değildir. Her adım ek kernel maliyeti eklendi, performans etkisi ölçülmedi.
- Sonraki iş: P0–P10 ana hedefini sürdür; NIST P5 kayıtlarındaki 800°C/2h işlem bilgisinin eksik metadata temsilini kaynak revizyonu/manifest etkileriyle düzeltme yolunu incele. P4 failed, P5 unavailable, P6/P7 partial durumları değişmedi.

## 2026-09-24 — NIST AMB2022-03 Table 4 kaynak revizyonu 1.1.0
- Resmî NIST yöntem belgesinin §2.1’i bare plate örneklerinin lazerlemeden önce vakumda 800°C/2h residual-stress anneal edildiğini belirtir; §2.5 BP1’i bu bare plate grubunda tanımlar. Yerel v1 aggregate dosyası değiştirilmeden v2/1.1.0 oluşturuldu; yeni alan ve kaynak konumu açıkça bağlandı, yedi koşulun tüm sayısal ölçümleri aynı kaldı. Manifest, source catalog, Python doğrulama, NIST API ve run-source hash allowlist’i yeni SHA-256’ya geçirildi.
- Windows NIST API alt sürecinde Python stdin artık açık UTF-8 çözümlüyor; aksi durumda derece işareti varsayılan kod sayfasında bozulup hash/metadata kapısı kapanıyordu. Dondurulmuş v1 protokolündeki artifact hash/path korunmuştur.
- Doğrulama: NIST kaynak/run API/UI/literature testleri **30/30 PASS**; Python comparison + resmi workbook aggregate testleri **9/9 PASS**; TypeScript `npm run lint` PASS. NIST kaynak metadata’sının düzelmesi P5 karşılaştırmasını geçerli kılmaz: plaka/SS304 temaslı termal sınır, 5° geliş açısı, ölçülmüş kaynak profili/bayt bağı ve model section operator eşleşmesi halen kapanmadı. P5 `unavailable` kalır.
- Sonraki somut adım: P5 için 3.17mm IN718 plaka ve tam temaslı SS304 taban sınırını model sözleşmesinde temsil etme fizibilitesini ve 5° lazer geometrisini kaynak profiliyle eşleştirmeyi araştır; kaynak yakalama, mesh veya kabul eşiği gevşetme.

## 2026-09-22 - Build-job Alaşım Kimliği Güvenlik Onarımı
- Build-job solver artık açıkça gönderilen desteklenmeyen `alloyId` değerlerini IN718'e sessizce düşürmek yerine hata ile reddediyor; alaşım belirtilmemesi durumundaki geriye dönük IN718 varsayılanı korunuyor.
- `Inconel 625` için desteklenmeyen surrogate hesaplamayı engelleyen Python regresyonu eklendi.
- Doğrulama: TypeScript lint PASS; izole Python solver sözleşme testi PASS. Tam `test_lpbf_build_job.py` Windows ortamında çıktı vermediği için tamamlanmadan durduruldu.

## 2026-09-22 - Ortak LPBF Fizik API'si
- Build-job Python Phase 5 hızlı suite'i PASS; desteklenmeyen alaşım kimliği regresyonu dahil.
- Malzeme özellik interpolasyonu, entalpi tablosu ve mesh-domain çağrıları `lpbf_core_physics.py` ortak giriş noktası altında toplandı. Transient solver, OpenFOAM adaptörü ve kanıt denetimi bu API'yi kullanıyor; eski registry dışa aktarımları uyumluluk için kaldı.
- Doğrulama: engineering 26 PASS / 1 OpenFOAM atlandı; heat-source 7 PASS / 1 atlandı; core-contract 8 PASS. Derlenmiş OpenFOAM çalıştırması mevcut ortamda yok. Sayısal kontroller başarılı.

## 2026-09-23 - Ortak LPBF Scan Schedule Çıkarımı
- `scan_segments` uygulaması `lpbf_core_physics.py` içine taşındı; `lpbf_simulation.scan_segments` eski import yolu olarak korunuyor. OpenFOAM, CFD, evidence ve test çağrıları geriye dönük uyumlu.
- Doğrulama: Windows Python 3.12 üzerinde `test_lpbf_core_contract` + `test_lpbf_engineering`: 33 PASS, gerçek OpenFOAM testi Windows worker gereksinimi nedeniyle SKIP. OpenFOAM 14 bağımsız parity testi Ubuntu 22.04 WSL'de ayrıca PASS. Tarama rotasyonu/dwell, stripe/island zamanlaması, enerji, entalpi ve solver sözleşmesi kontrolleri geçti.
- Commit: `b44210f` (`codex/lpbf-shared-core`). Sıradaki somut adım: hücre-integralli ısı kaynağı sınırını ayrı bir seam olarak çıkarmak.

## 2026-09-23 - Ortak LPBF Hücre-İntegralli Isı Kaynağı
- Gaussian hücre integrali, hücre ağırlıkları ve hareketli kaynak quadrature'ı `lpbf_core_physics.py` içine taşındı. `lpbf_heat_source` eski fonksiyon/sabit adlarını dışa aktarmaya devam ediyor; timestep sınırlayıcı ve iletim operatörü orada kaldı. Solverlar model kimliği `cell-integrated-gaussian-gl2-v1` değerini doğrudan ortak çekirdekten alıyor.
- Doğrulama: Windows Python 3.12'de core-contract + engineering + heat-source toplam 40 PASS / 2 Linux/OpenFOAM SKIP. Ubuntu 22.04 WSL'de OpenFOAM 14 parity testi ve ısı-kaynağı grubu 8/8 PASS.
- Commit: `2a53597` (`codex/lpbf-shared-core`). Sıradaki somut adım: ortak material authority ve SI/material adapter sınırını kaynak/caller kapsamıyla planlamak.

## 2026-09-23 - LPBF Malzeme Revizyon Kimliği
- Ortak registry'nin yeni malzeme snapshot'ları alaşım kimliği, açık provenance sınıfı, kimlik şema sürümü ve özellik içeriğinden türetilen SHA-256 revizyon özeti taşıyor. Mevcut `coreContract` tam malzeme nesnesini zaten bağlıyor; eski hash'siz kayıtlar okunabilir kalıyor.
- Hash içerik bütünlüğü/kimliği sağlar, bilimsel doğrulama veya kaynak güvenilirliği sağlamaz. Legacy özellikler `estimated-legacy`, kullanıcı tabloları `user-supplied-unverified` olarak etiketleniyor. Tedarik edilmiş snapshot'lardaki kimlik metadata'sı yeniden hesaplanıyor ve güvenilmiyor.
- Doğrulama: Windows Python 3.12'de core-contract + engineering + heat-source 44 PASS / 2 platforma özel SKIP. OpenFOAM 14 bağımsız parity testi Ubuntu 22.04 WSL'de PASS.
- Sıradaki somut adım: material authority ve SI/material adapter sınırını kaynak/caller kapsamıyla planlamak; EIS/EDS kapsam dışı kalıyor.

## 2026-09-23 - Ortak Termal SI Girdileri
- Kelvin preheat, metre cinsinden katman kalınlığı, m/s tarama hızı ve absorbe edilmiş lazer gücü dönüşümleri `thermal_si_inputs()` ile ortak çekirdeğe alındı. Referans transient solver ve termal OpenFOAM case üreticisi bu değerleri paylaşıyor; analitik tarama ve ayrı CFD modeli kapsam dışı.
- Doğrulama: Windows Python 3.12'de core-contract + engineering + heat-source 45 PASS / 2 platforma özel SKIP. WSL OpenFOAM 14 bağımsız parity testi PASS.
- Sıradaki somut adım: kalan adapter alanlarını (özellikle katman/beam geometri dönüşümleri) ancak birebir parity kanıtıyla değerlendirmek; EIS/EDS kapsam dışı.

## 2026-09-23 - Build-job Alaşım Eşleşmesi ve Cache Sözleşmesi
- Build-job ön yüzü yalnızca dört kilit alaşım için tam ad/alias allowlist'i kullanıyor ve specimen `baseMetal` alanıyla tutarlılığı denetliyor. CoCrMo, bilinmeyen, boş, çelişkili/bileşik, base-metal çatışmalı ve prototype adları reddediliyor; desteklenmeyen malzeme için 316L/IN718 literatür grafiği veya demo vektörü gösterilmiyor ve önceki job/UQ/NIST durumu temizleniyor.
- Python build-job API'si açık `alloyId` ile uyumsuz termal/slicer override'larını cache erişiminden önce reddediyor. Aynı alaşımın geçerli alias'ları solver'a kanonik adla gidiyor ve cache anahtarı kanonik kimliğe göre oluşturuluyor. Eksik/boş `alloyId` için mevcut IN718 varsayılanı korunuyor.
- Doğrulama: `python/test_lpbf_build_job.py` PASS; `tests/lpbf-build-session.test.ts` 8/8 PASS; `npm run lint` PASS. Prototype-property (`constructor`) ve eski cache hit regresyonları kapsandı.
- Sıradaki somut adım: worker sonucundaki `material.id` alanını ham istek yerine çözümlenmiş kimliğe bağlamak ve build-job'un fiilen kullandığı özellik snapshot'ı için ayrı, doğrulanabilir bir içerik kimliği tasarlamak. Bu hash deneysel doğrulama/provenance kanıtı sayılmayacak.

## 2026-09-23 - Build-job Worker Sonuç Kimliği
- Worker'ın `material.id` alanı artık ham `alloyId` isteği yerine solver'ın çözdüğü `alloyId` değerinden geliyor. Başarısız, çözümlenmemiş istekte alan `null` kalıyor.
- Doğrulama: Gerçek `lpbf_worker.py --execute` yolunda alias `Ti-6Al-4V` → `ti6al4v`, eksik alaşım → `in718`, desteklenmeyen `Inconel 625` → başarısız sonuç ve boş `material.id`. `git diff --check` PASS.
- Kod commit'i: `6f6c007` (`codex/lpbf-buildjob-material-identity`). Sıradaki somut adım: build-job'un fiilen kullandığı termal/slicer özellik snapshot'ı için ayrı içerik kimliğini tasarlamak; hash'i deneysel doğrulama veya provenance kanıtı olarak sunmamak. EIS/EDS kapsam dışı.

## 2026-09-23 - Geniş LPBF Goal Başlangıcı
- Kullanıcı ortak LPBF çekirdek/veri hattı hedefini GPU termal CPU eşleşmesi ve veri kapılı alaşım genişlemesiyle birlikte yetkilendirdi. Aktif yürütme planı `docs/LPBF_GOAL_EXECUTION_PLAN_2026-09-23.md`; goal kimliği `01a0cfbf-d2ad-7f70-b94f-b89183eb819c`.
- Salt okunur sınır incelemeleri build-job özellik/cache kimliği, run istemci/sunucu tür uyuşmazlığı ve eski Warp akış modelinin CPU entalpi-iletiminden farklı olduğunu doğruladı. Bağımsız Python malzeme, run istemcisi ve CPU yakınsama paketleri çalışıyor; dosya sahipliği `docs/ACTIVE_WORK.md` içinde.
- Plan/koordinasyon commit'i: `15a6ec8`. Kontrol: belge diff ve `git diff --check` PASS; bu plan paketinde solver veya ürün testi çalıştırılmadı. Sıradaki adım: üç bağımsız paket diff/kanıtlarını entegre etmek, ardından aynı termal sözleşmeye sahip GPU ve kaynaklı IN625 veri kapılarını açmak. Dışarıdan gelen belge değişiklikleri korunuyor; EIS/EDS kapsam dışı.

## 2026-09-23 - Geniş LPBF Goal Ara Kapısı
- CPU referans yakınsama harness'i 3 ağ + 3 zaman seviyesi, fiilî çözünürlük ve model/malzeme kimliği kapılarıyla eklendi (`94ddf92`). 8 odaklı kontrol PASS; gerçek IN718 pilotunda enerji kapanışı yaklaşık `3.05e-14`, eriyik W/D yakınsaması `inconclusive`. Bu deneysel doğrulama veya tamamlanmış P4 kapısı değildir.
- Dört alaşımın model yeterlilik matrisi ve IN625 için yalnız −18…982 °C katı-bulk `k(T)`/`Cp(T)` kaynak tablosu eklendi (`76d5506`); 2 kontrol PASS. Sıvı/yüksek sıcaklık ve optik veri eksikleri nedeniyle IN625 tam LPBF hesabına açılmadı.
- NIST AMB2022-03 IN718 optik yedi tek iz ölçümü çıplak levha ve D4σ olarak işaretlendi; toz katmanı/hatch takma değerleri kaldırıldı, uygunsuz karşılaştırma kapatıldı. 3B uzman laboratuvarı erişilebilir hale geldi (`246904c`); 2 regresyon ve TypeScript kontrolü PASS, gerçek tarayıcıda yedi vaka `Bare-plate model unavailable` olarak görüldü. Tablo 4 standart sapma aktarımı üzerinde çalışma sürüyor.
- NIST Table 4 yedi koşulun ortalama/standart sapma/n=6 verisi katalogda (`3376300`); odaklı test ve gerçek tarayıcı görünümü geçti. Bunlar yalnız ölçümdür, model kıyası değildir.
- Etkin build-job özellik snapshot'ı, SHA/cache/UQ ve ayrı IN625 AM-Bench screening snapshot kimliğiyle bağlandı (`887e398`); tam Python build-job testi PASS, gerçek UQ ve üç vakalık AM-Bench yolunda SHA görüldü. Hash bilimsel onay değildir.
- Açık `cuda:N` termal pilotu CPU ile aynı sınırlı problemi çözüyor (`37a56cd`); RTX 4060 pilotunda son 3B alan, enerji ve W/D paritesi kapıları geçti, 3 kontrol PASS. Ürün backend kaydı sürüyor; eski Warp modeli eşdeğer sayılmıyor.
- Exact kaynak revizyonlu run servisi (`b3292ee`) ve kaynak seçimi UI'si (`0daa456`) entegre edildi; sunucu ve istemci odaklı kontroller 7 ve 15 PASS. Sunucu-yerel bundle export/verify/izole restore API'si (`258032f`) 11 odaklı testte PASS. Bundle UI entegrasyonu sürüyor.
- Çıplak levha CPU modu ve +X iz ortası ever-liquidus W/D termal kesit operatörü (`f4e5ba0`) 13 ilgili testte PASS (1 platform SKIP); 3 ağ/3 zaman pilotunda enerji kapısı geçti, W/D yakınsaması `inconclusive`. NIST 10 mm izi, gerçek ışın profili ve dört optik kesit ortalaması çözülemediği için deney karşılaştırması `unavailable`; P4/P5 bilimsel kabulü açık.
- Sıradaki somut adım: GPU kuyruk adapterini ve bundle UI'sini entegre etmek; IN625 kaynak/model veri kapısını sonuçlandırmak; sonra birleşik sayısal/tarayıcı kontrolleriyle P4–P9 bitiş kararı vermek. Dış belge değişiklikleri korunuyor; EIS/EDS kapsam dışı.

## 2026-09-23 - LPBF Goal Kaynak ve GPU Kuyruk Entegrasyonu

- Sunucu-yerel run bundle arayüzü (`2c5b78f`) ve IN625 erime aralığıyla sınırlı entalpi taraması (`a221466`) eklendi. IN625 tam transient ve build-job desteği hâlâ kapalı; doğrudan melt-pool hesabındaki bilinmeyen malzemenin IN718'e sessiz düşmesi `3fe675a` ile giderildi.
- Ayrı `gpu-thermal-pilot` iş türü mevcut kuyruğa bağlandı (`98c33aa`). Açık `cuda:0` ile gerçek RTX 4060 kuyruk sonucu CPU paritesi PASS: son alan L2 bağıl farkı `2.10e-9`, azami bağıl fark `3.10e-9`, erime hacmi farkı sıfır. Python kuyruk/parite/worker 7 PASS; HTTP 1 PASS. Bu yalnız sınırlı tek iz, tek katman, toz katmanlı sayısal pilottur; kaynak integrali CPU'da, termal evrim CUDA'dadır. Arşiv yakalama ve üretim/deney geçerliliği unavailable.
- NIST AMB2022-03 optik Table 4'ün yedi koşulu yerel transkripsiyon olarak exact byte/SHA manifestiyle kaynak kataloğuna alındı (`34bd387`). Yayıncı ham dosyası/PDF olarak etiketlenmiyor. Kaynak önizleme/içe aktarma HTTP, bozuk hash ve eski revizyon testleri dâhil 5 odaklı test PASS; TypeScript lint PASS; ajan Chrome klavye görünümünü doğruladı. Kaynak kullanım şartları bilinmiyor olarak gösteriliyor. Ölçümle model kıyası ayrı kapıda açık.
- NIST optik karşılaştırma kapısı (`dffa0e5`) exact kaynak revizyonu/document SHA, yerel Tablo 4 içeriği, 10 mm +X çıplak levha, gerçek D4σ profil kanıtı, altı kesitlik optik operatör ve bağımsız 3+3 yakınsama olmadan sayısal hata üretmiyor. Beş Python testi PASS; gerçek 200 µm bare-plate pilotu `unavailable`, `errors:null`. Sentetik pozitif şema yalnız kapı mantığını sınar, deneysel başarı kanıtı değildir. Arşiv/API bağlantısı sürüyor.
- Yeni gerçek IN718 yakınsama denemesinde 60 W, üç ağ [45,30,20] µm ve üç zaman [4e-7,2e-7,1e-7] s: enerji kapanışı azami yaklaşık `3.03e-14`, fakat kaba ağda erime yok ve zaman boyunca ayrık W/D aynı. Dolayısıyla P4 `inconclusive`; NIST karşılaştırması da henüz `unavailable`.
- Sıradaki somut adım: GPU pilotu ayrı UI türüyle sunmak; kaynak revizyonuna bağlı NIST karşılaştırma raporunu entegre etmek; ardından P2/P4/P7 bilimsel kapıları ile birleşik P8/P9 kontrolünü sürdürmek. Dış dosya değişiklikleri korunuyor.

---

## 2026-09-22 - ML Veri Üretimi ve Meta-Model Genişletmesi
- Metalliksa kurallarına (sadece fiziksel temelli veriler) uygun olarak Ti-6Al-4V ve IN718 için grid-search tabanlı 1296 kombinasyonluk bir process map (`data/synthetic_process_map.csv`) üretildi (`python/generate_ml_dataset.py`).
- Faz 9 Surrogate Modeli, bu genişletilmiş fizik-tabanlı CSV verisini algılayıp rastgele verilerle birleştirerek hibrit, çok daha yoğun ve yüksek doğruluklu (OOD korumalı) bir eğitim süreci yürütecek şekilde güncellendi.
- **Sıradaki Adım (Production Readiness):** Geriye kalan "End-to-End CAD/Process Contract" (Faz 14 sonrası entegrasyonlar) veya Mekanik Tahmin Modellerinin (PINN ONNX) web için derlenmesi üzerinden ilerlemek.

## 2026-09-22 - Faz 14 UI ve STL Araçları
- Uygulama arayüzüne 3D Voxelization (hacimsel ayrıklaştırma) ve STL işleme araçları (Faz 14) entegre edildi. Git geçmişinde `feat: Add Phase 14 voxelization, STL tools and update UI` ile sabitlendi.

## 2026-09-21 - Faz 9 & 10 Arayüz Entegrasyonları
- AI Meta-Modelleri (Faz 9) ve Gumbel/Murakami Yorulma Ömrü (Fatigue - Faz 10) endpointleri, React UI tarafına bağlandı (`85f41b4`).
- Grup bazlı ayrılmış değerlendirme yapıldı ve modelin sınırları dışına (Out-of-Distribution) çıkıldığında sistemin anında "Confidence: 0.0" uyarısı vermesi sağlandı.

## 2026-09-21 - Faz 8 Hassasiyet ve Optimizasyon
- Backend'de yer alan `lpbf_bayesian_optimizer.py` modülü `server/lpbfWorkerBridge.ts` ve API üzerinden dışarıya açıldı. Optimum proses penceresi parametrelerinin arayışı yapılarak referans belgeler üretildi.

## 2026-09-23 - Frontend İngilizce Arayüz Metinleri
- Industrial Certification, Toolpath Thermal Map ve AI Orchestrator ekranlarındaki Türkçe kullanıcı metinleri İngilizceye çevrildi; Toolpath Thermal Map gezinme açıklaması da güncellendi.
- Doğrulama: `src/components` ve ilgili arayüz metadata'sında Türkçe karakter/ifade taraması yapıldı; kalan eşleşmeler yorum satırları veya bilimsel özel adlar. `git diff --check` PASS. Gerçek tarayıcı kontrolü yapılmadı.
- Kod değişiklikleri: `IndustrialCertificationLab.tsx`, `ToolpathThermalMapLab.tsx`, `AIOrchestratorPanel.tsx`, `workspaces.ts`.

## 2026-09-24 - LPBF Entegrasyon ve Bilimsel Kapı Durumu

- P4 için ayrık kabul metriklerinden ayrı liquidus konturu üretildi (`4092e06`). Yuvarlama ölçeğindeki geometri farkının yanlış yakınsama PASS üretmesi düzeltildi (`92a0e59`). Dondurulmuş 80 W IN718 3 ağ + 3 zaman raporu `docs/LPBF_CPU_CONVERGENCE_80W_2026-09-24.json` (`ec6b0a5`): altı koşu tamamlandı, enerji PASS, ağ genişliği inconclusive, ağ derinliği son iki seviyede %17,1875 farkla FAILED, zaman geometrisi inconclusive; toplam `failed`. Bu deneysel doğrulama değildir.
- NIST AMB2022-03 optik Tablo 4 karşılaştırmasında arşiv JSON sayılarının TypeScript tarafından yeniden yazılması iki sahte bütünlük uyarısı üretiyordu; ham doğrulanmış JSON metnini Python'a ileten düzeltme `3f152d1`. Gerçek arşivli IN718 pilotunda yalnız yedi gerçek koşul/operatör/yakınsama engeli kaldı; rapor `unavailable`, sayısal hata `null`.
- Canlı tarayıcıda Tablo 4 yerel transkripsiyonu revizyon 1 olarak içe alındı ve SHA baytları doğrulandı. 30 W, 1200 mm/s, 200 µm IN718 toz katmanlı kısa pilot `e8e26ffea4784e6288f7ddab5839de01` kaynak bağıyla arşivlendi. Bu pilot NIST'in 10 mm çıplak levha deneyine eşleşmez. 1 koşu, 62 artefakt ve 1 kaynak bağı içeren sunucu-yerel bundle `46c140da2e984c0ba00468eec09bdaf6` dışa aktarıldı, doğrulandı ve canlı arşiv değiştirilmeden ayrı kopyaya geri yüklendi. Yerel çalışma/bundle klasörleri `.gitignore` altında (`15309e6`).
- P7 dört alaşım ve IN625 veri kapısı matrisi GPU pilot kapsamına göre güncellendi (`8a046e3`). IN625 yalnız 273,15–1623,15 K füzyon-entalpi taramasına açıktır; tam transient/build-job hâlâ kapalı. UI'daki gerçek RTX 4060 CUDA pilotunda sayısal CPU/GPU paritesi PASS; deneysel doğrulama yok.
- P9: Windows Python LPBF birleşik paketi 91 PASS / 3 OpenFOAM SKIP; Ubuntu 22.04/OpenFOAM mühendislik paketi 29 PASS. TypeScript birim paketi 229 PASS; lint ve üretim build PASS. Envanterde iki eksik LPBF modülü kanıt sınırlarıyla kaydedildi (`2376aed`). Build yalnız mevcut büyük chunk uyarısını verdi.
- Yüzeye tam hizalı ikinci 80 W IN718 3+3 tanısı `docs/LPBF_CPU_SURFACE_ALIGNED_80W_2026-09-24.json` (`57e22ba`): altı koşu tamamlandı; kaynak yakalama >0,999999999, enerji PASS. Ağ W/D değerleri 40/40 → 40/40 → 60/50 µm; son çiftin genişlik farkı %33,33, derinlik farkı %20; zaman geometrisi inconclusive, toplam `failed`. Yüzey hizalaması tek başına P4'ü geçirmedi.
- NIST'in resmî optik kesit çalışma kitabı ve yayıncı SHA-256 yan dosyası `data/benchmark/nist-amb2022-03-optical/official/` altında ayrı tutuluyor (`ef303e7`); 25.811 baytlık XLSX'in SHA-256 değeri `2cfaac96aaca3dabb77b7029f842cdcc7e75c5a2cf3577d0734823246364a931` ve yan dosyayla eşleşiyor. BP1'deki 42 satır = yedi koşul × üç iz × 4,9/6,0 mm'de iki kesit; örnek ortalama ve standart sapma tüm Tablo 4 değerlerini 0,1 µm yuvarlamada yeniden üretti (Python 3/3 PASS). Ayrı `nist-amb2022-03-optical-xlsx-official-v1` kaynağı canlı tarayıcıda iki dosya/25.875 bayt olarak önizlendi, revizyon 1 içe alındı ve arşiv baytları doğrulandı. Yerel transkripsiyon revizyonu ve eski run bağı değişmedi. TypeScript 232/232, lint ve build PASS; build büyük chunk uyarısı sürüyor.
- Goal **aktif**. P4 ağ geometrisi yakınsamadı; P5 gerçek NIST optik hata hesabı için 10 mm deney koşulu, ölçülmüş ışın profili, uygun kesit operatörü ve bağımsız 3+3 yakınsama eksik. Tepe seçimi tanısı tamamlandı; mevcut kapılar değiştirilmedi. Sıradaki somut adım: Phase 22 pressure-projection operatörünü küçük üretilmiş CPU Warp alanında test etmek, ardından P5'e uygun optik kesit operatörünü ayrı kapsamda belirlemek. Dış `docs/README.md`, `sonkayıtlar/LOG.md` ve `docs/SCIENTIFIC_RESEARCH_VISION.md` değişiklikleri korunuyor. Push/yayın yapılmadı.
- Ek 5 µm, 588.544 hücreli 80 W tanısı `docs/LPBF_FINE_MESH_DIAGNOSTIC_80W_2026-09-24.json` (`2a5493a`): 375 s koşu tamamlandı, enerji hata payı `1,11e-14`; ayrık derinlik 10→5 µm'de 50→55 µm, %9,09 farkla mevcut %5 kapısını yine geçmedi. Keşif amaçlı sürekli liquidus konturu 20→10→5 µm'de W/D için yakınsama eğilimi gösterdi (son çift %1,27/%3,70), ancak yeni 3+3 kabul protokolü ve bağımsız zaman ekseni yok; P4 durumu değişmedi. Sıradaki sayısal adım: bu operatörü ayrı protokolde önceden tanımlayıp bağımsız süreç vektöründe ağ ve zaman eksenlerini tekrar ölçmek.

### P4 tepe-seçimi tanısı — 2026-09-24

- Aynı 75 W IN718 koşulunda tanı tekrarı, eşikler değiştirilmeden tamamlandı: `docs/LPBF_P4_PEAK_SELECTION_DIAGNOSTIC_75W_2026-09-24.json`, SHA-256 `bb7241cd328842517b8a0ad232c1cf160f0238ec459bbf66967ccc464b0ae4e`. Altı çözümün enerji kapanışı geçti; ayrık ve kontur kapıları `inconclusive` kaldı.
- Tepe seçicisi kabul edilmiş adımlardaki en büyük liquidus üstü aktif hücre sayısını buluyor ve eşitlikte ilk adımı seçiyor. Eşit maksimum penceresi 20/10/5 µm ağlarda 416/11/1 endpoint; seçilen zamanlar 161,950/177,417/168,667 µs. Sabit 10 µm ağda 1e-7/5e-8/2,5e-8 s adım sınırlarında seçilen zamanlar 177,467/177,417/177,367 µs; eşit maksimum sayısı 6/11/24. Bu seçim duyarlılığını ve ağ derinliğinin monoton olmamasına olası katkıyı gösterir, nedenselliği kanıtlamaz.
- Lineer kontur komşu aktif hücre merkezleri arasındaki liquidus kesişimlerini kullanır, yüzeye ekstrapolasyon yapmaz ve W/D'yi global projeksiyondan çıkarır. Bu yalnız sayısal termal vekildir; NIST optik kesit operatörüyle eşdeğer değildir. Zaman W/D farkları %0,004'ten küçüktür fakat monoton değildir; dondurulmuş trend ölçütü gereği PASS değildir. Odaklı Python regresyonları 18 PASS / 1 OpenFOAM SKIP; GPU koşusu yok. 80 W P4 `failed`, NIST P5 `unavailable` kalır.

### P5 10 mm çıplak levha hesaplanabilirlik denetimi

- Resmî NIST AMB2022-03 termografi Case 0 girdileri 285 W, 960 mm/s, nominal 67 µm Gaussian spot, 10 mm +X izi ve 23,5 °C altlık sıcaklığıdır. NIST'in 2025 beam metrology raporu bu koşul için ölçülmüş nominal 67 µm Gaussian `Dg` çapı ve %5,2 birleşik standart belirsizlik sağlar; ham 2B ışınım dağılımı artefaktı değildir. Table 4 D4σ → solver 1/e² eşlemesi açık yaklaşım, kaynak-bayt bağı ve belirsizlikle kaydedilmelidir.
- Mevcut `trackLength_um` üst sınırı 3000 µm olduğundan 10.000 µm Case 0, solver öncesi reddedildi. Yalnız geometri tahmini için sınır bellekte genişletildi; mevcut kare X/Y domain 20/10/5 µm'de 4.177.936 / 32.315.671 / 258.272.222 hücre ister. Hiçbir 10 mm termal çözüm çalıştırılmadı.
- Mevcut bare-plate operatörü tek x=0 kesitindeki ever-liquidus hücrelerinden W/D üretir. NIST optik sonuçları üç iz × iki kesitteki altı örnektir; kesitler başlangıçtan 4,9 ve 6,0 mm'dedir. Full-length kısıtı aşmak ve bu pozisyonları korumak için dar-bant/hareketli çerçeve veya ayrı uygun backend gereklidir. Ayrıntı aktif planda yer alır. Ölçülmüş profil ve geçen yakınsama gelene dek NIST residual yoktur.
- Ayrı Phase 21 2B geçici entalpi tarama motorunda liquidus entalpisindeki 4,53 K süreksizlik, `np.roll` kaynaklı karşı kenar ısı bağlantısı ve tek eksenli kararlılık hesabı düzeltildi (`69fae9a`); erime aralığında ön ısıtmanın başlangıç entalpisi de aynı faz yasasına bağlandı (`bf34aef`). Yalıtılmış sınırda korunumlu yüz akısı, iki eksenli zaman adımı ve katı/sıvı iletkenliği kullanılıyor. Motor hâlâ sabit ışınlı, hareketsiz, boyuna güç normalizasyonu çözülmemiş bir 2B taramadır; `is_physically_accurate=false`, kapsam ve yok sayılan `speed_m_s` çıktıda görünür. Odaklı Phase 21 + işçi testleri 6/6 PASS (Windows sandbox temp temizliği hatasından sonra normal izinlerle); P4 3B motoru bu değişiklikten etkilenmez. Sonraki adım: önkayıtlı 75 W bağımsız 3+3 kontur raporunu tamamlayıp mevcut P4/P5 kapılarıyla dürüstçe karşılaştırmak.
- Kullanıcının çekirdek motor fizik kusurlarını giderme yetkisi P10 olarak aktif plana eklendi (`04e0ea4`). Önceden sabitlenmiş bağımsız 75 W IN718 sürekli kontur 3 ağ + 3 zaman raporu `docs/LPBF_P4_CONTOUR_75W_2026-09-24.json` (`4110e73`, SHA-256 `b9cff92e01378a58ac4ed17333c21ad591a956dfc6e32ddd2dda8a219026b866`): altı koşu tamamlandı, model/solver/malzeme revizyonu aynı, enerji hatası azami `8,55e-14`. Ağ kontur W/D 55,99/47,93 → 60,02/52,41 → 62,31/52,13 µm; genişlik %3,67 son çift ve pozitif eğilimle PASS, derinlik monoton olmadığı için inconclusive. Zaman kontur W/D değişimi çok küçük fakat monoton değil; iki metrik inconclusive. Ayrık ve ayrı kontur toplamı `inconclusive`; orijinal 80 W dondurulmuş P4 `failed` sonucu değişmez, P5 NIST ölçüm kıyası hâlâ unavailable. Sonraki adım: mevcut tepe-anı seçiminin zamansal duyarlılığını ve gözlem operatörünü analitik/alan kanıtıyla araştır; eşikleri geriye dönük değiştirme ve gerçek deney uyumu olmadan karşılaştırma hatası üretme.
- NIST'in optik kesit ve metot kaynakları başlangıç 10 mm +X izinin nominal 67 µm Gaussian ışınını, ayrı dinamik eşleşme deneyinin ise 110 µm ışınını kullandığını doğruladı. 2025 NIST ışın metrolojisi ideal Gaussian'da D4σ ile 1/e² eşdeğerliğini, gerçek profilde sapma ve 67 µm koşulunda %5,2 birleşik standart çap belirsizliğini raporluyor. Bu nominal tarama girdisine kaynak sağlar; mevcut sıkı P5 kapısının istediği ölçülmüş profil artefaktı, deneyle eşlenmiş kesit operatörü ve geçen 3+3 raporunun yerini almaz. Arşivli Tablo 4 revizyonu ve run bağı değişmedi; P5 hata hesabı hâlâ unavailable.
- Kullanıcı yetkisiyle P10 Phase 22 Warp çekirdeğine genişletildi. `python/lpbf_transient_3d_gpu.py` içindeki 3B solver, işlem bitiş zamanını aşan fazladan adımı kaldırıp son `dt`'yi kırpıyor; sıfır süreli tek nokta izi artık ısıtma adımı yürütmüyor. Eğimli serbest yüzeyde x/y yüz akısı yalnız komşu hücre de metal ise hesaplanıyor; hava hücresine yapay iletim kesildi. Yeni Warp CPU regresyonları ve peak-seçim tanı regresyonları birlikte 8 PASS / 1 Windows/OpenFOAM SKIP; CUDA bulunmaması gerçek GPU doğrulamasını engelliyor. Basınç projeksiyonundaki merkezî D/G ile 7-noktalı Jacobi operatör uyuşmazlığı hâlâ çözülmedi; projection sayısal sözleşmesi yeniden tasarlanıp checkerboard/manufactured-field artakalanı ölçülmeden Phase 22 fiziksel kabul almayacak. CPU transient çekirdeğinde cut-cell yüzey yaklaşımı dokümante model sınırı olarak kaldı; bu turda değiştirilmedi.

## 2026-09-24 — Build-job peak kimliği ve Phase 22 taşınım/projeksiyon

- `e8f8313`: Rosenthal tepe sıcaklığı artık seçili sıcaklık alanının ışın merkezinden hesaplanıyor. Değişen build-job davranışı ayrı `solverRevision=lpbf-build-job-core-peak-field-v2` ile cache anahtarına/yanıtına/yetenek raporuna ve istemci same-session anahtarına bağlandı; fiziksel `modelId` korunuyor. Python build-job ve yetenek testleri PASS; peak tutarlılığı 3/3; TypeScript `tsc --noEmit` PASS. `tsx --test` oturum testi sandbox `spawn EPERM` nedeniyle çalıştırılamadı.
- `c902700`: Phase 22 MAC momentum çapraz hızları altı konumun tamamında dört komşulu interpolasyonla alınıyor. Entalpi adveksiyonu kapalı sınırları olan paylaşılan upwind yüz akılarının diverjansı; üretilmiş diverjanssız akışta global iç entalpi toplamı korunum testi eklendi. Basınç projeksiyonu yüz tabanlı ayrık D/G ile aynı Poisson stencil'ini kullanıyor; hava yüzleri p=0, katı/alan yüzleri geçirimsiz.
- CPU Warp + peak-field + capability birleşik testleri: **19 PASS**. Windows CUDA yolu yok; bu GPU donanım eşleşmesi kanıtı değildir. Warp geçici derleme klasörü kapanışında `WinError 5` uyarısı görüldü; testlerin kendisi exit code 0 ile tamamlandı.
- Basınç 10 Jacobi turunun fiziksel kabulü hâlâ başarısız/açık: smooth manufactured velocity için göreli L2 diverjans artığı 9³ ağda 0,420619; 17³ ağda 0,793338; 17³'te 300 turda 0,0378842 (hedef 1e-3). Runtime `unverified_residual_not_measured` döndürüyor; iterasyon bütçesiyle yakınsama iddiası yok. Sonraki iş: aynı stencil üzerinde cihaz indirgemeli PCG, post-projection artığı ve kapalı bileşen/nullspace testleri.
- Bu fizik onarımları P4/P5 bilimsel kararlarını değiştirmez: frozen 80 W P4 `failed`; NIST P5 `unavailable`. CPU transient tam-hücre yüzey yaklaşımı, Phase 22 sezgisel Marangoni/geri tepme serbest-yüzey yasası ve deneysel doğrulama eksikliği açık sınırlamalardır.

## 2026-09-24 — PCG pressure gate and P7 scope update

- Phase 22 production pressure projection now uses matrix-free Jacobi-preconditioned conjugate gradients over the repaired face-based D/G operator. Focused CPU Warp tests: **19/19 PASS**. Smooth manufactured fields reached the measured post-projection relative L2 divergence target 1e-3 at 9^3 (15 iterations, 7.606e-4) and 17^3 (35 iterations, 8.074e-4). Tests cover anisotropic pressure, free surface, solid inclusion, Neumann compatibility/nullspace, disconnected components, iteration exhaustion, and numerical breakdown.
- Runtime status is residual-based; it reports per-step maximum and total iterations, measured linear residual and post-projection divergence. Incompatible Neumann components report `numerical_failure`. GPU-device execution/performance remains **unverified** because CUDA is unavailable on this host.
- P7 remains **partial**. IN625 is admitted only to explicitly unvalidated literature-model fusion-enthalpy screening (273.15–1623.15 K); full transient and build-job remain closed. Source-validity range, uncertainty, and exact material/process-state correspondence are unresolved, so this capability does not satisfy the full P7 acceptance gate.
- TypeScript build-job failure handling fix `f06ba5e`: `npx tsc --noEmit` PASS and focused session behavior tests 11/11 PASS in the elevated runner. Earlier sandbox `spawn EPERM` did not establish a product failure.
- P4 frozen 80 W remains `failed`; NIST P5 remains `unavailable`. Remaining physics gaps include heuristic Phase 22 Marangoni/recoil laws, CPU whole-cell free-surface geometry, and lack of experimental validation. Next: audit and repair remaining core physics defects while preserving the current gates, then continue P5 observation-model feasibility. No push/publication.

## 2026-09-24 — Phase 21 core conduction and remaining gates

- Phase 21 `_conduction_rate` now converts integer-valued temperature/conductivity arrays to floating-point fields before accumulating fluxes; the old integer `zeros_like` path could truncate fractional heat flow to zero. Regression checks signed nonzero rates, floating dtype, and internal-flux conservation. Focused Phase 21 suite: **6 PASS**; `git diff --check` PASS.
- CPU reference transient read-only audit found no additional defensible conservation/boundary defect. Two manufactured heterogeneous-conductivity checks now pass: operator symmetry/internal-flux cancellation, integrated half-cell base plus top convective-radiative boundary balance, and timestep refinement convergence. These verify the numerical operator, not experimental accuracy or complete phase-change coupling.
- GPU alloy expansion was audited but not enabled: available CUDA parity covers the existing IN718 case only. IN625 lacks a complete source-backed transient property table and optical/flow inputs. Keep the capability closed until those inputs and same-physics GPU parity are established.
- Core physics work from this continuation is recorded in `11334ff` (Phase 22 pressure PCG), `96eeca2` (Phase 21 conduction dtype), and `e692038` (heterogeneous CPU conduction tests). Full-scale CUDA parity/performance, P7 full data gate, frozen P4, NIST P5, and other constitutive limitations remain open. No push/publication.

## 2026-09-24 — Surface-force regime and GPU parity update

- Phase 22 now limits Marangoni stress to the liquidus–boiling interval and recoil to temperatures above boiling, based on Alphonso et al. 2023, DOI `10.1016/j.jmapro.2023.03.040`. Commit `3ff4c9e` replaces the independent recoil-to-Bernoulli surface recession with a post-projection height-graph kinematic update from projected U/V/W plus the same Hertz–Knudsen evaporation flux used by enthalpy cooling; upper free-surface pressure boundary is exposed to projection. The focused regression passes and the Phase 22 CPU Warp suite passes **24/24**. CUDA execution remains unverified; interface breakup/reformation and plume resolution remain outside this height-graph model.
- CUDA thermal pilot parity now includes actual `cuda:0` execution for both IN718 and 316L. Targeted suite: **4/4 PASS**. 316L parity is bound to the existing `estimated-legacy` material revision and remains unvalidated/non-production; no source-backed alloy gate was opened.
- An opt-in rectangular bare-plate corridor now preserves 10 mm X scan geometry and reports actual rectangular resource shape; the 600,000-cell guard remains enforced. Commit `7e5437e` adds versioned thermal-proxy sections at 4.9 and 6.0 mm from the +X scan start, with cell-center selection or linear interpolation between accepted-step peak-temperature fields; tests: 11 PASS. Each record identifies only one simulated line. No 10 mm solve ran; NIST's measured 67 µm nominal `Dg` is known, but no source-byte-bound `Dg`/D4σ model mapping, three experimental-line identities, boundary-width sensitivity, or independent 3+3 exists, so P5 stays `unavailable`.
- In the same continuation, `9322b8f` records source-aligned Phase 22 surface-force temperature regimes and a CUDA-gated 316L test; actual `cuda:0` parity passed for the existing estimated snapshot. The TypeScript/UI path does not expose rectangular bare-plate geometry.

## 2026-09-24 — Physics scope and bounded CUDA smoke
- Kullanıcı çekirdek motorlardaki kanıtlanabilir fizik kusurlarının düzeltilmesine de yetki verdi; bu kapsam aktif P10 planına eklendi. Faz/entalpi, 3B adveksiyon-momentum ve kaynak geçerlilik kapısı işleri ayrı dosya sahiplikleriyle paralel yürütülüyor (`docs/ACTIVE_WORK.md`).
- Faz 22 serbest-yüzey kinematik kernel'i küçük sabit girdiyle hem CPU Warp hem `cuda:0` üzerinde çalıştırıldı. Merkez yüzey yüksekliği iki cihazda da `2.5191626264131628e-05 m`; maksimum mutlak fark `0`. Bu yalnız kernel smoke/paritesidir; bağlı CUDA projeksiyon/recoil solver'ının yürütme ve performansını doğrulamaz.
- Dikdörtgen koridorda 1 mm, 20 µm CPU koşusu 25.200 hücre ve 4.267 adımda 24,47 s sürdü; göreli enerji hatası `6.53e-14`. 10 mm koşusu yürütülmedi. Tam genişlik duyarlılığı ve P5 deney kıyası açık kalır.
- Faz 22 eğimli arayüz Marangoni gradyanı artık aynı z düzlemindeki hava/metal hücrelerini karıştırmak yerine komşu sütunların yüzey hücrelerini örnekliyor (`39a6f8f`); odaklı regresyon 1/1, 3B suite 25/25 PASS. Bu hâlâ yükseklik grafiği üzerinde sınır kuvveti yaklaşımıdır.
- Buharlaşma enerji akısı ile yükseklikten hesaplanan kütle kaybı eğimli yüzeyde farklı alan kullanıyordu. Entalpi akısına aynı `sqrt(1+h_x²+h_y²)` yüzey metriği eklendi (`52b51cb`); analitik testte enerji/kütle/H-K hesapları %1 içinde, Phase 22 suite 26/26 PASS. Tam bağlı Phase 22 CUDA çözümü doğrulanmadı.
- CPU Gaussian kaynak ağı, yakalanmayan enerjiyi tam absorbe güce ölçekliyordu. `ee730ec` düşük yakalamayı (yakalanan oran `<1/1.01`) reddediyor; 20 µm koridorun %38,3 yakalaması reddedildi, 320/480 µm kabul edildi. CPU odaklı paket 29 PASS / 1 OpenFOAM SKIP.
- Aynı kaynak kapısı CUDA pilotuna taşındı (`46b9be6`); eşik tek `MINIMUM_SOURCE_CAPTURE_FRACTION` ve iki backend ortak doğrulama kullanıyor. CUDA pilot 5/5 PASS (RTX 4060 üzerinde IN718 ve estimated-legacy 316L parity), ısı-kaynağı paketi 8 PASS / 1 OpenFOAM SKIP. 316L sonucu kaynaklı yeterlilik değildir.
- `9325765` isteğe bağlı `sourceValidityRange_K` alanını tablo `temperatureCoverage_K` değerinden ayırdı. Varsa tüm tabloyu ve beyan edilen kaynama sıcaklığını kapsamak zorunda; yoksa kaynak aralığı unknown kalır. Eski four-alloy ve supplied IN718 hash'leri alan yokken korundu. Mühendislik paketi 32 PASS / 1 OpenFOAM SKIP, `npx tsc --noEmit` PASS; termal UI'da canlı erişilebilirlik/klavye kontrolü PASS. Yeni alaşım kabulü açılmadı.
- Entalpi/faz incelemesinde hata doğrulanmadı. CPU ve GPU aynı entalpi tablosu, sabit referans yoğunluğu ve latent ısı yasasını kullanıyor; `test_lpbf_gpu_thermal.py` 5/5 CUDA dahil PASS. P5 genişlik duyarlılığı protokolü (`a065b77`) 10 mm / 320, 480, 640 µm koşularını kaynak ve ayar hash'leriyle önceden kaydetti. 480 µm referansı doğrudan CPU'da başlatıldı; 133 kaynak-adımı/7,23 s sonra modelin kaynama geçerlilik sınırında durdu. Son adım kaynak yakalaması %99,999999998; tam iz sonucu, enerji/kesit veya karşılaştırma residual'ı üretilmedi. P5 hâlâ unavailable; [geçerlilik duruşu kaydı](docs/p5_case0_10mm_480um_validity_stop.json).
- Bağlı Phase 22 `solve_toolpath` aynı tek adımlı girdiyle gerçek RTX 4060 `cuda:0` ve CPU'da çalıştı; her ikisi de 7 PCG iterasyonunda yakınsadı. En yüksek sıcaklık farkı 0,000244 K, hız farkı 1,69e-8 m/s, eriyik hacmi ve keyhole derinliği farkı sıfır. Bu yalnız küçük smoke; tam ölçekli bağlı CUDA parity/performance kanıtı değildir.
- IN625 kaynak kapısı taraması, Gen3 CSP katı-bulk ölçümleri, farklı bileşimli BÖHLER L625 sıvı levitasyon ölçümleri, toz DRS absorptivitesi ve NIST AMBench lot kimliğinin birbirini tamamladığını ancak aynı lot için kaynama aralığında geçerli LPBF tablosu vermediğini gösterdi. Yeni tam transient/GPU alaşımı açılmadı; kaynak kararı [P7 raporunda](docs/IN625_P7_SOURCE_GATE_2026-09-24.md).

## 2026-09-24 — OpenFOAM iki-faz enerji taşınımı düzeltmesi
- `metalliksaMeltPoolFoam` enerji denkleminde önceki yüzey akısı, metal/gaz hacim-ağırlıklı `cp` alanını toplam `rhoPhi` kütle akısıyla çarpıyordu. Foundation OpenFOAM 14'ün [resmî üst çözücüsü](https://github.com/OpenFOAM/OpenFOAM-14/blob/master/applications/modules/incompressibleVoF/incompressibleVoF.C) `rhoPhi = rho1*alphaPhi1 + rho2*alphaPhi2` kuruyor; bu nedenle farklı yoğunluklu fazların `cp` katkıları arayüzde yanlış ağırlıklandırılıyordu.
- Düzeltme `rhoCpPhi = rho1*interpolate(cpEffMetal)*alphaPhi1 + rho2*cpGas*alphaPhi2` kullanıyor. Böylece metalin mushy apparent heat capacity'si kendi faz hacim akısıyla taşınırken gazın özgül ısısı gaz akısında kalıyor. Kod ve dar test `13d1f05` commit'inde.
- `python/test_lpbf_openfoam_thermal_flux.py`: 2/2 PASS; test üretim ifadesini statik bağlar ve saf/mixed faz, mushy kapasite ve işaretli akı oracle'ını sayısal denetler. `py_compile` ve `git diff --check` PASS. Bu test OpenFOAM çalıştırması/derlemesi değildir. Yerel OpenFOAM entegrasyonu WSL servis `E_ACCESSDENIED` nedeniyle doğrulanamadı.

## 2026-09-24 — Phase 22 çok adımlı CPU/CUDA parity
- `python/test_lpbf_phase22_multistep_cuda_parity.py` üretim `solve_toolpath` yolunda dondurulmuş 5 adımlı durumu CPU ve açık RTX 4060 `cuda:0` üzerinde çalıştırıyor; odaklı test **1/1 PASS**. Son sıcaklık aynı (2533.48999 K), eriyik hacmi ve yüzey çekilmesi aynı; maksimum hız farkı yaklaşık `4.77e-7 m/s`. Her iki projeksiyon da yakınsadı; göreli post-projection L2 `7.34e-8` / `1.03e-7`.
- Ayrı kanıt: `docs/PHASE22_CPU_CUDA_MULTISTEP_SMOKE_2026-09-24.md`. Bu küçük tek vakanın kanıtlamadığı şeyler: tam alan paritesi, enerji kapanışı, performans, ağ/zaman yakınsaması ve deneysel geçerlilik. Maksimum mutlak diverjans `0.078125` / `0.15625 s⁻¹` olarak farklı; bu fark gizlenmedi.
- Kök tekrarında sistem Warp cache ACL'si nedeniyle ilk deneme çalışmadı; yazılabilir workspace cache'iyle hedefli test geçti. Test sonu Warp/PCH geçici klasörü silme uyarısı verdi (WinError 5); test exit code 0. CUDA geniş kapsamlı yeterlilik ve P6 kabul kapısı hâlâ açık.

## 2026-09-24 — Phase 22 tam alan CPU/CUDA tanısı
- `solve_toolpath(..., include_diagnostic_fields=True)` yalnız açıkça istendiğinde son T/H/U/V/W/P/yüzey alanlarını verir; varsayılan sonuç şeması değişmez ve 100.000 hücre üstü tanı isteği reddedilir.
- Aynı dondurulmuş 5-adımlı vaka CPU ile RTX 4060 `cuda:0` arasında bütün alanlarda sabit toleransları geçti; odaklı paket **3/3 PASS** (alan paritesi, varsayılan şema, boyut kapısı). En büyük T farkı `2.4414e-4 K`, H göreli L2 `1.96e-8`, basınç göreli L2 `6.92e-7`, yüzey yüksekliği farkı sıfır. Ayrıntılar: `docs/PHASE22_FULL_FIELD_CPU_CUDA_PARITY_2026-09-24.md`.
- Bu solverda kaynak/taşınım/radyasyon/buharlaşma terimleri için denetlenebilir tam enerji defteri yok; enerji kapanışı unavailable. Sonuç tek küçük vaka içindir; analitik doğruluk, yakınsama, performans ve deney doğrulaması sağlamaz. P6 kısmi.

## 2026-09-24 — Phase 22 CUDA bağımsız basınç kontrolü
- Üretim CUDA PCG'si, 9³ dondurulmuş sıvı ağında bağımsız host sonlu-hacim `A=-D(G)` operatörünün ürettiği imal edilmiş basınç çözümünü geri buldu. Açık RTX 4060 `cuda:0`, hedefli **1/1 PASS**; lineer residual `7.4851e-4`, gauge-ayarlı basınç göreli L2 hatası `2.9145e-4`, bağımsız operatör residual'ı `7.4854e-4` (her biri ≤`1e-3`).
- Bu yalnız ayrık basınç operatörünün tek-manufactured-case kontrolüdür; enerji kapanışı, yüzey kuvvet yasaları, mesh/time yakınsaması veya deney kıyası değildir. [Rapor](docs/PHASE22_CUDA_PRESSURE_MANUFACTURED_2026-09-24.md).

## 2026-09-24 — Eğimli yüzey enerji kaybı ve build-job worker onarımı
- Phase 22 GPU entalpi güncellemesinde konveksiyon/radyasyon gerçek yüzey alanı metriğiyle ölçeklenmiyor, buharlaşma ise ölçekleniyordu. Kayıpları tek `sqrt(1+h_x²+h_y²)` metriğiyle yüzey alanına bağladım; lazer girdisi izdüşüm alanında kaldı. Ayrı enerji defteri tüm uygulanan terimleri ve maske sıfırlamasını sayıyor. Commit `a344821`; odaklı paket 28/28 PASS. RTX 4060 `cuda:0`, 64³, 43 adımda enerji kapanış bağıl hatası `9.724e-5`; basınç artıklarının ikisi `1e-3` altında. [Rapor](docs/PHASE22_CUDA_ENERGY_AUDIT_2026-09-24.md). Bu tek bounded CUDA koşusudur; ağ/zaman yakınsaması veya deney geçerliliği değildir; P6 kısmi kalır.
- Bağımsız CPU çekirdek denetiminde `enthalpy-fv-6` hattında yeni yüksek güvenli fizik kusuru bulunmadı; enerji, iç akı korunumu ve kaynak kırpılması mevcut testlerle bağlı. Legacy 2D transient FDM endpoint'inin tarama hızı kullanmaması ve sabit kaynak kabulleri belgelenmiş ekranlama sınırlarıdır; ana solver'a karıştırılmadı.
- Build-job worker, eksik `timeout_s` alanını zorunlu okuyup alt süreci başlatırken hata veriyordu. 300 s varsayılanı ve regresyon eklendi (`python/lpbf_worker.py`, `python/test_lpbf_worker_timeout.py`). Odaklı test 1/1 PASS; güncel checkout sunucusunda iki gerçek API işi `completed` oldu ve ikisi de `in718` + özellik SHA-256 snapshot'ı döndürdü. `Queue.capture` çıktısı alındı ancak `legacy-unbound`; tam kaynak bağlama, kalıcı arşiv, dışa aktarma ve geri yükleme zinciri açık.
- P4 tied-endpoint tanısı (`docs/LPBF_P4_TIED_ENDPOINT_DIAGNOSTIC_75W_2026-09-24.json`) bağımsız kontur oracle'ıyla 3 ağda eşleşti; kilit kabul değerleri değişmeden kaldı. 75 W durum `inconclusive`, frozen 80 W P4 `failed`.
- Run arşivine `build-screening` / `transient-thermal` sınıfı eklendi; eski şemasız v1 kayıtları değişmeden okunup `legacy-unspecified` gösteriliyor. Sınıf worker çıktısından capture, preview/import, get/list, UI ve bundle restore doğrulamalarına taşınıyor. NIST servisi build-job kayıtlarını (legacy build-job dahil) `unavailable` tutuyor; `coreContract` üretilmiyor. Doğrulama: Python capture/gerçek worker e2e 7/7, odaklı TS arşiv/NIST/client/bundle 31/31, lint PASS. Gerçek build-job kaydının seçili tarayıcı akışında dışa aktarılıp ayrı depoya geri yüklenmesi hâlâ birleşik uçtan uca kanıt değil.

## 2026-09-24 — P5 Case 0 fizik uygunluğu yeniden denetlendi
- NIST'in Case 0 koşulu 285 W, 960 mm/s, 67 µm dönel Gaussian, +X 10 mm tek iz ve 23,5 ± 1 °C başlangıç sıcaklığıdır. Kesit çalışması her koşul için 3 iz × 2 kesit = 6 ölçüm kullanır; metin “yaklaşık orta” konum der, uygulamadaki 4,9/6,0 mm sabitleri kaynakta açıkça doğrulanmadı. [NIST AMB2022-03 yöntem belgesi](https://www.nist.gov/document/amb2022-03-measurement-and-challenge-descriptions-version-101), [NIST kesit geometrisi makalesi](https://tsapps.nist.gov/publication/get_pdf.cfm?pub_id=957295).
- Mevcut `enthalpy-fv-6` Case 0 denemesi 133 kaynak adımı ve 7,23 s sonunda kaynama geçerlilik sınırında durdu; taramanın yalnız %0,1024'ü yürüdü. Tam iz, enerji kapanışı, optik kesit veya residual yok. NIST tablosu Case 0 için 2,1 en-boy oranı raporlar. Aynı solverı tekrar çalıştırmak veya koridor genişliğini değiştirmek P5'i açmaz; durum `unavailable` kalır.
- Önerilen sonraki bilimsel adım: kaynama üstü malzeme yanıtını, enerji/kütle uyumlu buharlaşmayı ve evrilen serbest yüzeyi içeren yeni solver revizyonunu belirlemek; bu koşul için keyhole/recoil gereksinimini değerlendirmek; ardından ayrı protokolle 3×3 ağ/zaman çalışmasını dondurmak. Bu, mevcut NIST karşılaştırması için önkoşul önerisidir, henüz uygulanmış veya doğrulanmış bir model değildir. 2025 NIST ışın metrologisi 67 µm nominal Gauss girdisine belirsizlik eklemeyi destekler; sıkı byte-bound 2B profil kapısını tek başına karşılamaz. [NIST AMS 100-67](https://tsapps.nist.gov/publication/get_pdf.cfm?pub_id=958616).
- Kod haritası: CPU `enthalpy-fv-6` sabit serbest yüzeyli iletim/faz-değişimi modelidir; buharlaşma, yüzey kütle kaybı, recoil ve akış çözmez. Phase 22 bunların bazılarını height-graph üzerinde heuristik olarak içerir; OpenFOAM VOF yoluysa varsayılan Ti-6Al-4V buharlaşma parametrelerine dayanır ve IN718 mass/energy bağlaşımını kanıtlamaz. Hiçbiri bugünkü haliyle P5 Case 0 solverı diye sunulamaz. Kaynama guard'ı korunur.

## 2026-09-24 — Bağımsız ısı oracle'ı ve arşiv UI akışı
- Phase 22 üretim ısı kernel'i için analitik 3B Dirichlet modu ve solver-defterinden bağımsız tam alan enerji integrali eklendi. CPU 9³/17³/33³ ve RTX 4060 `cuda:0` 33³ hataları monoton azaldı; hedefli test 2/2 PASS. Ayrıntı: `docs/PHASE22_MANUFACTURED_THERMAL_2026-09-24.md`. Yalnız sabit özellikli saf iletim; P6 kısmi.
- Ayrı port ve depolarda canlı tarayıcı akışı NIST workbook/Table 4 preview→import→verify, IN718 screening→source-bound archive, comparison, bundle export→verify→isolated restore adımlarını tamamladı. Comparison bilimsel kapılar nedeniyle unavailable kaldı. Run ayrıntıları `PROOF.md` içinde.
- Analitik screening için yanlış `transient-thermal` sınıflaması `analytical-screening` runKind ile düzeltildi. Yeni izole UI tekrarında gerçek koşu doğru türle önizlendi/arşivlendi; Table 4 revision 1 bağı korundu, NIST kapısı analitik model için unavailable döndürdü, bir-run/one-source bundle export→verify→restore başarılı oldu. Odaklı doğrulama: Python 10/10, TypeScript 35/35, `npm run lint` ve `git diff --check` PASS. CPU, CUDA/Warp ve faz/ısı-akısı denetimlerinde yeni yüksek güvenli fizik hatası bulunmadı; OpenFOAM buharlaşma kütle kapanışı kalifiye değil. Commit `2dd15f5`. Bu sonuç P6/P5 bilimsel kabulünü kapatmaz.

## 2026-09-24 — P1 bileşik kimlik, P7 IN625 oracle'ı, P10 CFD kapısı
- Build-job özellik hash'i ayrı kaldı; yeni `buildJobIdentity` kanonik alaşım, `rosenthal-screening-v1` model kimliği, solver revizyonu, özellik snapshot şema/revizyonu ve özellik hash'ini bağlıyor. Tam digest cache anahtarına giriyor; cache hit ve TypeScript sonuç sınırında alan tutarlılığı denetleniyor. Alias eşitliği ve model/revizyon/şema değişimleri test edildi.
- IN625 entalpi ekranı için bağımsız 12 noktalı Gauss–Legendre Cp integrali; H(T), mushy aralık latent katkısı, faz sınırlarında süreklilik, monotonluk ve sonlu fark dH/dT oracle'ı eklendi. 290 kJ/kg katkı tam denetleniyor. Bu sayısal implementasyon kanıtı, materyal modelinin fiziksel/deneysel geçerliliği değildir; kaynak geçerlilik aralığı bilinmiyor, build-job/tam transient kapalı.
- OpenFOAM buharlaşma akısının latent enerji kaybı ve recoil/plume kuvvetinde kullanıldığı, fakat VOF/continuity kütle aktarımının bulunmadığı doğrulandı. Üretim çok-fizik vaka üretimi ve C++ örtük varsayılanı bu terimleri kapatıyor; analitik recoil fikstürü yalnızca formül/yön doğrulaması için açık. Tanılar kütle-transfer kapanışını `absent` ve modeli `unqualified` kaydediyor. Tam evaporatif VOF fiziği henüz uygulanmadı.
- Doğrulama: build-job identity script'i ajan çalıştırmasında PASS; `python -m unittest test_in625_lpbf_material test_lpbf_cfd.TestEvaporationMassClosureGate -v` 7/7 PASS; build-job/CFD Python `py_compile` PASS; build-session TypeScript 12/12 PASS; `npm run lint` PASS; `git diff --check` PASS. OpenFOAM derlemesi/yürütmesi yapılmadı. Kökün ek build-job tam-script tekrarı sandbox dışı komut süresini uzattığı için durduruldu; ajan aynı script'i tamamlayıp PASS bildirdi.
- Plan kullanıcının GPU alaşım genişletme isteğiyle güncellendi: en az bir yeni alaşımda GPU termal yolu ve her GPU'ya açılan alaşım için aynı model yasasıyla CPU/GPU yeterlilik şartı var. IN625 GPU uygulanabilirlik denetimi açık. P4/P5/P6 ve genel goal açık; bu dilim onları kapatmıyor.

## 2026-09-24 — IN625 sınırlı bare-plate CPU/CUDA yolu
- Yeni `python/in625_bareplate_field.py` yolu, ortak IN625 snapshot revizyonuyla sınırlı 3B iletim/entalpi taraması yapıyor. Açık absorbed-W girdisi, sabit 8440 kg/m³ lot-eşleşmemiş yoğunluk varsayımı, altı adyabatik yüz, hareketli Gauss yüzey girdisi ve 273.15–1623.15 K sert sınırı sonuçta görünür.
- Üretim entalpi malzeme revizyonu `f47b07e4c8288b8c7177001f069a254be3410bace43ad5ea2f73168ac4466f07`; status `unvalidated-literature-model-screening`. CPU/CUDA 576 hücre/4 adım dondurulmuş vaka RTX 4060 `cuda:0` üzerinde alan-parite farkı 0 K / 0 J/kg, CPU ve CUDA enerji artığı `1.11e-16 J`. CUDA smoke süresi 1.325 s, ayrılmış tepe bellek 100,864 B; ölçek/performance iddiası değildir.
- Kaynak yakalama eşiği `1/1.01` ortak `lpbf_heat_source.py` modülüne taşındı; mevcut CPU solver aynı sabiti import eder. IN625 sınır kirişini yeniden ölçeklemeden önce ortak gate ile reddeder.
- Kapability raporu IN625 bare-plate yolunu ve dar CPU/CUDA numerical-only statüsünü listeler; build-job/slicer, powder-bed, genel full transient ve deneysel geçerlilik kapalıdır. NIST AMB2018-02 geometrisiyle actual karşılaştırma yapılmadı.
- Doğrulama: IN625 field + constitutive CUDA + shared heat source: **36 PASS, 1 platform/OpenFOAM SKIP**, capability suite **4/4 PASS**, Python syntax ve `git diff --check` PASS. Ayrıntı: `docs/IN625_BAREPLATE_GPU_SCREENING_2026-09-24.md`. P4/P5/P6 ve geniş hedef açık.

## 2026-09-24 — Çekirdek fizik düzeltmeleri ve IN625 UI/arşiv entegrasyonu
- Phase 22 eğimli serbest yüzeyde Marangoni sıcaklık gradyanını yüzey metriğiyle teğetsel U/V/W kuvvetine dönüştürüyor; normal hız üretmiyor. Geri tepme itkisi artık yerel yükseklik-grafiği normalinin içine yöneliyor; düz yüzey sınırında önceki dikey yön korunuyor. Ayrıntı `python/lpbf_transient_3d_gpu.py` ve ilgili test fikstürleri.
- İkinci fizik denetimi, teğetsel kayma büyüklüğünde kalan bir geometri hatası buldu: `mu*du_t/dn=tau` koşulunda dikey `dz`, normal örnek aralığı yerine kullanılıyordu. Marangoni güncellemesi şimdi `dz/sqrt(1+h_x²+h_y²)` normal uzaklığını kullanıyor; eğimli-yüzey regresyonu hem teğetselliği hem çıkarılan gerilme büyüklüğünü denetliyor.
- Normal-uzaklık düzeltmesi sonrasında tam Phase 22 Python suite'i yeniden çalıştırıldı: **29/29 PASS**, CUDA kernel'leri gerçek RTX 4060 `cuda:0` üzerinde derlenip çalıştı. Bu testler sayısal/kernel tutarlılığıdır, deneysel doğrulama değildir.
- IN625 bare-plate arayüzünde CPU/CUDA sonuç alanları JSON sanmak yerine `float64` little-endian binary olarak alınıp byte uzunluğu, content type ve SHA-256 doğrulanıyor. 1,152 hücre / 11 adım aynı ayarlı gerçek UI koşusunda CPU ve RTX 4060 `cuda:0` alan SHA'sı aynı; sıcaklık alanı karşılaştırması **max 0 K, RMS 0 K**. Skaler karşılaştırmada entalpi, enerji artığı, yakalama ve enerji defteri farkı sıfır.
- Yerel IN625 kaynak revision 2 (2 dosya, 1.938 byte; doküman SHA-256 `be3286b30b3ec3a6970b577cd9050b19de716cbf8754c7ac2355cf20d5cea655`) preview→import→byte verify akışından geçti. CUDA run `587632c4976349e0b0d2a11718f62d3e`, 3 artifact ile aynı kaynak revision'ına bağlanıp arşivlendi; server-local bundle 1 run/1 source link ile export→verify→restore edildi. Kayıt core contract'ı hâlâ `legacy-unbound`; NIST Table 4 comparison bu IN625 screening run için `unavailable` kalır.
- Kod, testler, kaynak paketi ve kanıt checkpoint'i commit `a5d60b6` içinde; kullanıcıya ait `docs/README.md`, `sonkayıtlar/LOG.md` ve `docs/SCIENTIFIC_RESEARCH_VISION.md` commit dışında korundu.
- Run archive API'si yakalanmış `result.settings` üzerinden geçici doğrulama zarfını kuruyor; capture şemasına bulunmayan `requestSummary` zorunluluğu eklenmedi. Kaynak allowlist'inde IN625 kabul edildi. Run HTTP preview round-trip **2/2 PASS**, kaynak/HTTP odaklı testler **10/10 PASS**, komşu arşiv/import/API testleri **16/16 PASS**; TypeScript, lint ve diff denetimleri PASS.
- Geçerli arayüz/model hâlâ açıkça `unvalidated-literature-model-screening`: yoğunluk lot eşleşmeli değil, kaynak kapsamı türetilmiş screening input'larıdır; deney/NIST uygunluğu, genel transient/build-job ve alloy qualification anlamına gelmez. P4 frozen 80 W başarısız, P5 kullanılamaz, P6 ve P7 kısmi; bunlar açık kalır.
- Dört mevcut alaşım ile sınırlı IN625 yolunun otorite/kapsam özeti `docs/LPBF_ALLOY_CAPABILITY_MATRIX_2026-09-24.md` içinde. Makinece okunabilir tek otorite `material_capability_report()` olarak kaldı; matris hiçbir eski girdiyi source-qualified saymıyor.
- Kullanıcı 2026-09-24'te goal'u duraklatıp devam noktasını kaydetti; ardından devam edilmesini istedi. Güncel devam sırası `docs/ACTIVE_WORK.md` içindedir. Normal-uzaklık düzeltmesi ve capability matrix ayrı, odaklı kanıt paketi olarak tutuluyor.

## 2026-09-24 — IN625 mushy-range CPU/CUDA ekranı
- P6'daki yeni alaşım GPU yolunun yalnız katı bölgede denenmiş olmasını gidermek için sentetik faz-aralığı vakası eklendi: 8×8×2 hücre, 250 µm, 1500 K yapay başlangıç, 30 W açık absorbe güç, 1e-4 s × 33 adım; toplam 0.099 J, Gaussian kaynak yakalaması 0.9982844950.
- CPU ve RTX 4060 `cuda:0` 1565.4608746 K tepeye ve aynı dört mushy hücreye ulaştı; sıcaklık alanı max farkı 4.55e-13 K, özgül entalpi farkı 2.33e-10 J/kg. Bağımsız 64 noktalı Cp integrali ve başlangıç entalpisi + absorbe enerji toplamı iki backend'de geçti; ledger artığı 5.33e-15 J.
- `python/test_in625_bareplate_field.py`: **10/10 PASS**; yalnız pytest cache ACL uyarısı var. Tek-run profil CPU 0.289 s, CUDA 9.196 s; incremental CUDA allocated memory 24,576 B. Bu küçük problem CUDA'da daha yavaştır; bu bir hızlanma iddiası değildir.
- Bu vaka kaynak seçimi sırasında CPU parametre keşfiyle belirlendi; kör/preregistered deney değildir ve 1500 K başlangıç gerçekçi LPBF preheat'i olarak yorumlanmamalıdır. Yalnızca sınırlı JMatPro-tabanlı screening yasasının mushy aralığını alan çözücüsünde uygular. IN625 kaynak/uncertainty kabulü ve genel transient admission kapalı; P6/P7 kısmi kalır. Ayrıntı: `docs/IN625_MUSHY_CPU_CUDA_SCREENING_2026-09-24.md`.
- Bağımsız kabul denetimleri sonucu: frozen 80 W P4 `failed`; ayrı 75 W kontur sonucu `inconclusive`. P5 `unavailable`; Case 0, sabit materyal kaynama guard'ında taramanın %0,1024'ünde durdu ve aynı solver/protokol tekrarlanmamalı. Yeni source-matched, kütle/enerji kapalı free-surface model olmadan NIST Case 0'a ilerlenmez.
- P6, IN625 screening yasasının mushy alanını artık 128 ve 1.024 hücrede CPU/CUDA sayısal olarak kapsıyor; 11/11 test PASS. Tek koşu GPU/CPU süre oranları sırasıyla 31,8× ve 17,7× yavaş, kaynak kapısı P7'yi geçmiyor. P7 kaynak taramasında Hastelloy X en yakın aday, ancak ergime üstü iletkenlik ölçümü değil 15× model varsayımı; CoCrMo kaynaklarında uyumlu kimya/durum ve tam katılaşma datası gösterilemedi. Ayrıntılı P4/P5/P6/P7 kabul sırası `docs/ACTIVE_WORK.md` içinde.
- Phase 22 fizik düzeltmesi: sıvı yüzey buharlaşma akısı için solidus eşiği entalpi kaybı, yükseklik/kütle kaybı ve enerji ledger'ında ortaklaştırıldı. 1850 K / 1878 K regresyonu önce eski kodda entalpi kaybını gösterip, düzeltmeden sonra üç terimin sıfır olduğunu doğruluyor; üst-solidus alan/kütle eşleşmesi korunuyor. `test_lpbf_transient_3d_gpu.py`: **30/30 CPU Warp PASS**. Yalnız yerel pytest/Warp geçici-dizin ACL uyarıları var; GPU yürütmesi bu pakette çalıştırılmadı. Ayrıntı `PROOF.md` ve `docs/ACTIVE_WORK.md`.
- Phase 22 zaman adımı düzeltmesi: açık momentum tahmini için yalnız termal difüzyon sınırı yetersizdi. Yeni ortak seçim `dt`'yi termal sınır ile hız bileşeni 5 m/s üst sınırını ve `nu=mu/rho` viskozitesini kullanan `dt·Σ(|u|/h + 2nu/h²) ≤ 0,5` muhafazakâr kapısının küçüğü yapıyor. Düşük termal yayınımlı testte momentum sınırı belirleyici; tam-zaman scheduler testi de geçiyor. `test_lpbf_transient_3d_gpu.py`: **31/31 CPU Warp PASS**; yerel pytest/Warp ACL uyarıları test sonrası. Bu sayısal kararlılık iyileştirmesidir, momentum/free-surface modelinin deneysel doğrulaması değildir. Ayrıntı `PROOF.md`.

## 2026-09-24 — Hastelloy X P7 source gate
- Scheel et al. supplies useful as-deposited LPBF Hastelloy X Cp/enthalpy, but the liquid conductivity is a 15× modeling assumption, density is a fixed room-temperature measurement, and the full property uncertainty/melting-range set is not closed. NASA AM context and NIMS liquid-density metadata do not close these gaps or match into one source revision.
- Decision: no P7 full-range pass and no runtime alloy admission. A clearly labelled bounded numerical pilot can be considered only with measured Cp/latent heat separated from assumed liquid properties. Source limits and links are recorded in docs/HASTELLOY_X_P7_SOURCE_HUNT_2026-09-24.md.
- P4 remains failed, P5 unavailable, and P6 partial. Next: continue scoped physics closure and larger same-physics GPU crossover analysis; parity alone does not qualify the alloy or process.

## 2026-09-24 — Phase 22 fail-closed transport boundaries
- The projected explicit transport CFL is now checked against actual U/V/W before enthalpy updates; a projected field over the conservative 0.5 momentum/advection bound stops with a validity error. An evaporating molten interface reaching the z=2*dz height-graph floor likewise stops before enthalpy and ledger updates, avoiding unpaired loss at a clipped surface.
- Synthetic regressions cover a 10 m/s projected velocity and molten evaporation at the lower graph floor. Full Phase 22 suite: **33/33 PASS** on CPU Warp. Warp temp cleanup emitted a Windows ACL warning after tests; GPU execution was not performed in this run.
- NIMS experiment 264's public page does not expose specimen chemistry IDs, numeric density values/uncertainty, or sample-specific surface-tension/viscosity fits. NIMS documents those outputs for its method generally, but this record does not establish them. No files were downloaded. Detailed source boundary: docs/HASTELLOY_X_P7_SOURCE_HUNT_2026-09-24.md.
- Next: run a warm-up plus repeated, alternating CPU/CUDA timings on the same IN625 workload; separate solver and end-to-end timing, and make no speedup claim before that evidence.


## 2026-09-24 — IN625 warmed repeated CPU/CUDA timing
- After CUDA enthalpy inversion synchronization reduction (150e604), one warm-up per backend and five alternating repeats gave median CPU/CUDA times of 0.198/4.877 s at 128 cells and 1.359/19.099 s at 1,024 cells. CUDA remained 24.6× and 14.1× slower respectively; temperature differences stayed below 9.1e-13 K.
- The focused field suite passed 12/12 after the change. This measurement includes direct solver/result construction, excludes UI/API transport, and is not a paired before/after campaign. It supports no speedup or crossover claim. Full method and ranges: docs/IN625_CUDA_BENCHMARK_2026-09-24.md.
- Next: retain CUDA for explicit numerical parity only; profile larger same-physics workloads before pursuing kernel fusion. P6/P7 remain partial and alloy qualification remains closed.

## 2026-09-24 — Larger same-domain IN625 GPU screen
- Added a 2,048-cell z-refined synthetic case over the same 2×2×0.5 mm domain, 3.3 ms, 30 W and 0.099 J input. CPU/CUDA medians: 4.013/43.617 s (3 alternated repeats; CUDA 10.87× slower). Both reached 1593.516258 K and 44 mushy cells. Independent enthalpy/energy oracles passed; max field deltas 9.10e-13 K and 4.66e-10 J/kg.
- This verifies same-law numerical consistency at one larger, anisotropically refined screen, not process convergence, alloy qualification, or GPU crossover. Detailed method: docs/IN625_CUDA_BENCHMARK_2026-09-24.md.
- Current next action: decide whether to implement fused/compiled kernels only after a backend-level profile identifies launch-bound versus arithmetic-bound time; otherwise prioritize source/data and model-closure gates. P6/P7 remain partial.

## 2026-09-24 — Phase 22 guard CUDA execution follow-up
- The two new boundary/CFL regressions now run on CPU and explicit cuda:0 when available. Focused GPU-aware run: **2 tests passed, 4 device subtests passed** on NVIDIA GeForce RTX 4060 Laptop GPU.
- This compiles and executes the floor-hit fail-closed path and actual projected-velocity CFL kernel on CUDA. It does not amount to whole-process experimental or general solver validation. Existing P4/P5/P6/P7 statuses are unchanged.

## NIST P5 geometry feasibility audit — 2026-09-24

A focused feasibility audit confirms the current `stationary-enthalpy-conduction-v1` cannot represent the source specimen: its domain uses a minimum 300 µm homogeneous IN718 substrate, fixes the bottom at initial temperature, and has no SS304 backing/contact interface. Increasing depth alone would still model the support as IN718. The solver heat source is a normal-incidence symmetric Gaussian; `scanAngle_deg=0` describes path direction, not incidence. NIST's 5 ± 0.5° applies to the Table 1 thermography/optical setup, while 8° belongs to a separate dynamic-coupling condition. No AMB2022-03 measured irradiance-map artifact was found in the cited dataset package.

Decision: preserve v1 identity and keep P5 unavailable. A credible follow-up requires an opt-in versioned layered-plate/support model with explicit contact and bottom-boundary assumptions, and explicit incidence angle/azimuth in the source contract. Do not bind a presumed Gaussian as measured profile evidence. No new P5 run until these source-matching gates are closed.

## NIST P5 support/profile source boundary — 2026-09-24

NIST's public method PDF identifies the IN718 coupon as 3.17 mm thick and 25.4 × 25.4 mm, residual-stress annealed at 800 °C for 2 h, and clamped in full bottom contact to an SS304 baseplate. It also reports backside temperature rise below 0.1 °C after each single-track scan. Public sources reviewed do not specify the SS304 baseplate thickness, contact conductance/resistance, clamp preload, or the holder's bottom/edge thermal boundary. Do not substitute the separate AMB2022-01 substrate geometry or silently assume these missing values.

The AMB2022-03 profile hunt found no gate-bindable measured irradiance-map file in the official optical/thermography packages or the NIST beam-metrology report. Other benchmark profile CSVs are from different apparatus/conditions and cannot be reused. Therefore P5 remains unavailable pending exact stack/contact evidence and the measured-profile artifact; bounded sensitivity models can be developed but must remain explicitly unvalidated.

## SS304 support property snapshot scaffold — 2026-09-24

Added `python/lpbf_ss304_support_material.py` as a separate generic AISI 304 literature model, not as an admitted production alloy or a specimen-specific AMB2022 baseplate revision. It records the NIST report's density, Cp and conductivity fits, source locations and a deterministic material revision hash; evaluation is bounded to 273.15–1473.15 K and reports unvalidated literature-model status. Focused verification: **4/4 PASS**. It is not yet wired into the thermal solver. The NIST holder's actual material certificate and stack/contact properties remain unknown.

## 2026-09-24 — Layered plate v2 pilot integration
- Added an opt-in CPU reference path `layered-plate-enthalpy-v1` for a single bare IN718 plate plus generic SS304 support. Explicit plate/support thickness, interface resistance, support-bottom boundary, oblique assumed-Gaussian beam identity and source penetration bind into core-contract schema v2. Existing v1 behavior and identity are retained.
- The layered finite-volume face transfer is conservative; the support property snapshot is separately hashed and bounded to 273.15–1473.15 K. This is an unvalidated sensitivity model, not an AMB2022-03 source-matched result. Actual support certificate/geometry/contact/bottom boundary and measured irradiance profile remain unavailable, so P5 stays unavailable.
- Verification: layered plate + heat source + layered conduction **24 PASS / 1 platform skip**; v2 core-contract focused checks **2/2 PASS**; Python compile, TypeScript typecheck and `git diff --check` PASS. TypeScript runtime test is blocked by Windows Node/esbuild `spawn EPERM`; one broader saved-binding test is blocked by Windows SQLite temp ACL errors.
- Layered v2 implementation is committed as `9a95cce`; the fused Warp IN625 inverse is committed as `0c15487`. The focused field suite passed **13/13**, including a direct phase-boundary inverse test, and three synchronized alternating 2,048-cell CUDA pairs: Torch **52.972 / 33.084 / 34.247 s**, Warp **2.097 / 1.356 / 1.530 s**. Peak T, 44 mushy cells, total enthalpy and energy residual matched within reported precision. Timing varied about 1.6× within each backend, so this supports a repeatable improvement only for this workload; it is not an application-wide speedup claim. GPU numerical parity is limited to the bounded IN625 bare-plate model; P6 remains partial and this does not validate the alloy or process. Next: audit/close the new-alloy data gate and run the full select→compute→compare→export→restore workflow acceptance. P5 remains unavailable. `graft build` remains blocked by an ACL-denied old run-shape temp directory; source-level queries still return indexed spans.
# 2026-09-25 — NIST proxy campaign contract hardening

Added a standalone versioned validator for three archived computational tracks,
each with 4.9 mm and 6.0 mm thermal-proxy sections. It binds source/run/input/
material identities and rejects optical/etched or experimental-validation
claims. Hardening also rejects boolean schema versions, malformed trusted source
identities, and absolute/traversing artifact paths. The report explicitly marks
numerical convergence not evaluated and comparison residuals absent.

Verification: `python -m unittest test_lpbf_nist_proxy_campaign -v` **12/12
PASS**; targeted `git diff --check` PASS. This is a structural contract only:
it does not run a solver, read/verify archive bytes, integrate a section
operator, calculate residuals, or establish convergence/validation. The current
LPBF transient cores remain subject to the existing documented physics and
validation gates. Next: integrate or explicitly reject this contract at the
archive/API boundary, then perform a focused numerical defect audit of the
CPU-reference and Phase 22 GPU engines before choosing a bounded correction.

# 2026-09-25 — Phase 22 parity-test repair and engine audit

The CUDA full-field and multistep parity tests pinned an obsolete five-step
count. The current adaptive stability limit executes 19 steps for the frozen
6 µs case; both tests stopped before their CPU/CUDA acceptance checks. Removed
the stale fixed count and now require positive/equal backend step counts before
comparing the fields.

Verification: Phase 22 thermal/hydrodynamic, energy-ledger, manufactured
thermal/pressure, multistep parity, and full-field parity suites: **42/42 PASS**
on NVIDIA RTX 4060 `cuda:0`. The full-field test compared temperature, enthalpy,
velocity, pressure, and surface fields. Legacy Phase 21 focused physics tests:
**6/6 PASS**. Broader CPU engineering/heat-source/core-contract run: 62 tests,
8 errors, 2 skips; the errors were SQLite database creation/cleanup permission
failures under Windows temporary directories, so the broad suite is not a PASS.

Read-only CPU core audit found conservative shared-face fluxes, manufactured
spatial convergence, enthalpy/latent-heat checks, and bounded energy closure;
no reproducible defect in `enthalpy-fv-6` was demonstrated. Phase 22 operators
likewise showed no reproducible defect in this reviewed scope. Both remain
screening/prototype models with documented missing experimental validation;
Phase 22 free-surface Marangoni/recoil conditions are heuristic boundary laws.
A remaining CPU verification gap is production-path temporal convergence: the
current temporal refinement test has a separate local update loop. Next, design
an independent transient manufactured check through the production stepping
path, then continue the physics/data/API goal without relaxing P4/P5/P6/P7 gates.

## 2026-09-25 — CPU transient final-time roundoff and 3×3 recheck

A real IN718 CPU time-refinement run exposed an endpoint artifact: requested
50 ns and 25 ns levels took 7,001 and 14,001 steps and reported minimum steps
of `5.18e-17 s` and `4.34e-19 s`, respectively, for a 350 µs end time. The
final step was only accumulated-time floating-point residue. The loop now
snaps to the requested final time when remaining time is within a step-count-
scaled ULP tolerance, capped at `1e-14 s`; no thermal update, source scaling,
or balance equation changed. A regression first reproduced the tiny step,
then passed after the fix.

The same fixed IN718 case was re-run with layer-conforming meshes 40/20/10 µm
and requested time caps 100/50/25 ns. Actual time steps were now distinct at
100/50/25 ns with exactly 3,500/7,000/14,000 steps; minimum steps stayed at the
requested scale. Maximum energy relative error was `1.63e-13`. CPU convergence
gate remains **failed** for mesh (depth 40/20/30 µm; 20→10 µm changes 33.3%)
and **inconclusive** for time because thresholded width/depth remained identical
across levels. Peak temperatures were 2795.36/2793.79/2793.74 K but are not part
of the pre-frozen acceptance criteria. Verification: focused production energy,
source-free, multilayer, heterogeneous refinement, and convergence contract
tests **15/15 PASS**. This numerical study is not experimental validation.

Next: retain the failed/inconclusive P4 result; diagnose cell-edge/thresholded
geometry resolution or define a prospective physical observable and protocol.
Do not alter the existing acceptance thresholds or relabel the study as passed.
