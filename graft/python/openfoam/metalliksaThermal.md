# python/openfoam/metalliksaThermal.C

- Segment · class · L18-L18 — struct Segment { double a,b,x0,y0,x1,y1,surface; };
- interp · function · L19-L27 — double interp(const std::vector<Row>& rows, double x, int xcol, int ycol)
- begin · function · L21-L21 — auto it = std::lower_bound(rows.begin(), rows.end(), x,
- end · function · L21-L21 — auto it = std::lower_bound(rows.begin(), rows.end(), x,
- lower_bound · function · L21-L22 — auto it = std::lower_bound(rows.begin(), rows.end(), x,
- main · function · L28-L176 — int main(int argc, char *argv[])
