# python/openfoam/metalliksaThermal.C

- Segment · class · L19-L19 — struct Segment { double a,b,x0,y0,x1,y1,surface; int track,layer; };
- gaussianMass · function · L21-L29 — double gaussianMass(double lower, double upper, double centre, double radius)
- interp · function · L30-L38 — double interp(const std::vector<Row>& rows, double x, int xcol, int ycol)
- begin · function · L32-L32 — auto it = std::lower_bound(rows.begin(), rows.end(), x,
- end · function · L32-L32 — auto it = std::lower_bound(rows.begin(), rows.end(), x,
- lower_bound · function · L32-L33 — auto it = std::lower_bound(rows.begin(), rows.end(), x,
- main · function · L39-L39 — int main(int argc, char *argv[])
